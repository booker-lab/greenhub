# 육안 검증 다음 대화 핸드오프

> 작성일: 2026-05-31
>
> 목적: 다음 대화에서 로그인된 Chrome 세션을 이어받아 순차 육안 검증을 계속한다.
>
> **상태**: 상세 실행 이력으로 보존한다. 다음 대화의 재개 진입점은 `visual-verify-handoff-20260601.md`다.

## 1. 첫 진입 절차

1. `Chrome` 스킬을 사용한다.
2. 확장 브라우저 목록에서 `metadata.profileName === '정연'`인 Chrome을 선택한다.
3. 열린 사용자 탭에서 `https://seller.greenlove.co.kr/orders` 또는 같은 도메인의 탭을 찾는다.
4. 해당 탭을 이어받아 읽기 전용 항목부터 검증한다.
5. 브라우저 식별자는 재실행 시 달라질 수 있으므로 문서의 과거 식별자를 재사용하지 않는다.

Chrome 연결 상태:

- Codex Chrome Extension `v1.1.5` 설치 및 연결 완료
- 연결 가능한 프로필: `Jo`, `정연`
- 판매자 로그인 세션이 있는 프로필: `정연`
- 직접 접근 확인 완료: `https://seller.greenlove.co.kr/orders`

## 2. 검증 원칙

- 순차 진행판: `manual-visual-verify-checklist.md`
- 세부 판정 원본: `pending-visual-verify.md`, `pending-visual-verify-20260529.md`
- 실패·보완 누적: `visual-verify-fix-backlog.md`
- 운영 데이터 상태 변경은 사용자 확인 없이 실행하지 않는다.
- 읽기 전용 화면, 탭 이동, 필터, 모달 열기·닫기는 Codex가 직접 진행한다.
- 시각적으로 애매한 항목과 운영 쓰기 항목만 사용자에게 확인을 요청한다.

## 3. 현재 완료 또는 부분 완료

### 판매자 정산

- `pending-visual-verify.md` §1-V `#1~#4`, `#17` 통과
- 확인 완료: 5개 상태 탭 렌더·문구·순서·초기 `전체` 활성, CSV 다운로드
- `pending-visual-verify.md` §1-V `#8~#16` 통과
- 확인 완료: 상태별 필터, 로딩 위치, `전체` 복귀, 빈 결과와 빈 결과 상태의 탭 유지
- 잔여: `#5~#7` 모바일 가로 스크롤

### 판매자 주문

- `pending-visual-verify-20260529.md` §16 `#155` 통과
- `pending-visual-verify-20260529.md` §16 `#156~#157` 통과
- `pending-visual-verify-20260529.md` §17 `#166` 통과
- `#165`는 확인 모달 노출까지 확인, 취소 시 API 미호출은 잔여
- 운영에서 일괄 준비 1건 처리 완료: 처리 필요 `3→2`, 대기 중 `2→3`
- `pending-visual-verify-20260529.md` §18 `#168~#172` 통과
- 일괄 택배 발송은 직배송 2건 미노출·택배 1건만 선택 가능, 모달 필수값 차단, `기타` 직접 입력, 탭 이동 후 선택 초기화까지 확인
- 운영 쓰기인 `#173` 실제 발송 완료는 사용자 승인 전 보류

## 4. 발견된 보완 작업

- `VF-001`: 소비자 홈 배너 헤드라인과 이미지 겹침
- `VF-002`: 판매자 주문 일괄 준비 액션 바와 카드의 체크박스 아래 큰 검은 체크 표시 중복
- `VF-003`: 종결. Railway 운영 API 직접 배포 후 신규 `/banners/active` 200, 구 `/banner` 404, 어드민 `/admin/banners` 복구
- `VF-004`: 겸직 계정 사업자 프로필 수정 빈 폼 원인 확정 및 로컬 수정 완료. API `GET/PATCH /stores/:storeId`의 `admin` 소유권 우회 누락을 보정하고, `/onboarding` 조회 실패 노출·저장 차단을 추가했다. 배포 후 재검증 필요
- `VF-005`: 운영 API는 `/banners/active`에서 `main_hero`를 반환하지만 소비자 홈 데스크톱·`375px` 화면에는 배너가 전혀 렌더링되지 않는다. 로컬 최신 코드는 `<HeroBanner />`를 조립하지만 Vercel 최근 소비자 배포는 모두 `READY`, `target: null`이다. 최신 미승격 배포는 보호 URL에서 `Failed to fetch`가 표시된다. 운영 승격 또는 CORS 변경은 사용자 승인 전 금지

### 어드민 배너

- `pending-visual-verify-20260529.md` §28 `#248` 통과
- 확인 완료: 최신 목록형 UI의 `새 배너` Drawer 열기·닫기, 이미지·텍스트·기간·CTA·라이브 미리보기 노출
- `pending-visual-verify-20260529.md` §27 `#245`, §28 `#246~#248`, §31 `#267` 통과
- 확인 완료: 운영 API 배포 복구, 기본 배너 목록 1건, 기본 배너 수정 전용 보호, 구 `/banner` 404
- 재검증 실패: §29 `#255`, §30 `#265`, §31 `#266`, §32 `#268`은 소비자 홈 배너 미노출로 `VF-005` 추적

### 겸직 계정 역할 분리

- `pending-visual-verify.md` §3 `#45~#48` 통과
- 확인 완료: 설정 → `/admin/stores` → 헤더 `셀러 화면으로` → `/orders` 왕복
- 잔여: `#42`는 VF-004 로컬 수정 완료 후 배포·읽기 전용 재검증 필요. 저장 검증 `#43`은 보류

### 어드민 소비자

- `pending-visual-verify.md` §5 `#68`, `#72` 통과
- 확인 완료: 소비자 계정 새로고침 로딩 표시, 로딩 중 검색 입력과 `전체·정상·정지` 탭 유지
- 운영 소비자 목록은 현재 0건이라 가입일·전화·검색 매칭·상태별 행 필터 검증은 보류

## 7. 2026-05-31 Railway 운영 API 직접 배포

- 사용자 승인 후 Railway CLI를 현재 프로젝트 `enchanting-enjoyment` 하나로 제한 인증했다.
- 작업 트리의 사용자 변경을 섞지 않기 위해 커밋 `14426f8`의 임시 Git 작업 트리를 만들고 `api` 운영 서비스에 직접 업로드했다.
- 배포 ID: `f422b2ba-4cad-4b4c-aea4-d86bd6641789`
- 결과: `SUCCESS`
- 운영 확인: `/health` 200, `/banners/active` 200, `/banner` 404, 인증 없는 `/admin/banners` 401, 로그인 어드민 `/admin/banner` 기본 배너 1건

## 8. 다음 대화 권장 진입점

1. Chrome `정연` 프로필에서 seller에 다시 로그인한 뒤 겸직 계정 `/onboarding`에서 기존 난플렉스 값이 채워지는지 읽기 전용으로 재검증한다. 저장은 금지한다.
2. 모바일 `375px` 뷰포트 제어가 가능한 브라우저로 판매자 정산 `#5~#7`, 주문 모바일 배치, 공개 배너 `#260`의 VF-001 겹침을 묶어 확인한다.
3. 운영 상태 변경이 필요한 주문 발송·프로필 저장·어드민 액션은 사용자 승인을 다시 받은 뒤 별도로 진행한다.

## 10. 2026-05-31 판매자 주문 읽기 전용 재개 결과

- `/orders` 상단 알림의 `처리 필요 보기`, `지연 주문 보기` 이동을 다시 확인했다.
- 지연 주문 화면에서 직배송 2건은 체크박스가 없고, 택배 주문 `RESET-reset-order-parcel` 1건만 일괄 택배 발송 선택이 가능함을 확인했다.
- 택배 주문 1건을 선택한 뒤 상태 필터 `대기 중`을 누르면 선택 건수 `1건`과 활성화된 `택배 발송` 버튼이 유지되는 결함을 재현했다. API 호출은 없었다.
- 신규 보완 작업 `VF-006`으로 기록했다. 구현 수정은 육안 검증 보완 묶음에서 진행한다.
- `/settlements` 주문별 상세에서 `정산 대기` 빈 결과와 탭 유지, `확정` 2건, `지급 완료` 1건, `취소` 1건, `전체` 4건 복귀를 다시 확인했다.
- `/onboarding`에서 겸직 계정의 기존 상호명·대표자명·연락처·소재지가 정상 조회됨을 확인했다. 사업자등록번호는 기존 저장값이 없는 선택 항목이라 빈 값이다. 저장은 실행하지 않았다. `VF-004`를 종결했다.
- 공개 소비자 홈 `https://greenlove.co.kr`에서 `VF-001`을 데스크톱 `1280x720`, 모바일 `375x812`로 다시 확인했다. 양쪽 모두 헤드라인이 오른쪽 이미지 위로 겹치며, 모바일은 이미지 영역 안에서 줄바꿈된다.

세부 내용은 `visual-verify-fix-backlog.md`를 따른다. 검증 중 새 문제가 나오면 다음 번호로 누적한다.

## 11. 2026-05-31 육안 검증 보완 묶음 1차 로컬 수정

- `VF-001`: 이미지가 있는 소비자 배너의 데스크톱 텍스트 폭을 왼쪽 `50%`로 제한하고, `480px` 이하에서는 이미지 `140px`와 텍스트를 세로 적층했다.
- `VF-002`: seller 선택적 Mantine CSS import에 `Checkbox.css`를 추가했다.
- `VF-006`: 판매 유형·상태 탭·날짜 필터·배송 중 하위 필터·우선 알림 이동 시 일괄 선택을 초기화하고, 실시간 자격 제거 효과는 유지했다.
- 검증: 변경 코드 Biome 0, seller·consumer 타입체크 0, seller 주문 상수 테스트 6/6, consumer·seller 빌드 0.
- 로컬 consumer를 운영 공개 API에 읽기 전용으로 연결해 데스크톱 `1280x720` 텍스트·이미지 분리, 모바일 `375x812` 이미지 아래 텍스트 적층, 가로 넘침 0을 확인했다.
- 운영 배포와 운영 재검증은 수행하지 않았다. 다음 진입점은 세 항목의 배포 후 운영 읽기 전용 재검증이다.

## 12. 2026-05-31 육안 검증 보완 묶음 1차 운영 종결

- consumer 운영 배포 `greenhubconsumer-3qzqh5rdq...`가 완료되어 `https://greenlove.co.kr` 별칭에 연결됐다.
- seller 운영 배포 `greenhub-seller-4p3cn2a57...`가 완료되어 `https://seller.greenlove.co.kr` 별칭에 연결됐다.
- `VF-001`: 운영 consumer 데스크톱 `1280x720`과 모바일 `375x812`에서 텍스트·이미지 분리와 가로 넘침 0을 확인해 종결했다.
- `VF-002`: 운영 seller 지연 주문 목록에서 액션 바와 카드 체크박스 내부 아이콘 정상 크기·위치를 확인해 종결했다. 어드민 정산은 운영 목록 `0건`이라 행 체크박스 실물은 없었다.
- `VF-006`: 운영 seller에서 `RESET-reset-order-parcel` 1건 선택 후 `대기 중` 탭 전환 시 선택 `0건`, 액션 비활성, 체크박스 미선택 복귀를 확인해 종결했다. API 호출은 없었다.

## 13. 2026-05-31 판매자 주문 상세 버튼 재검증과 VF-007

- 운영 seller `RESET-reset-order-faraway` 상세에서 `준비 시작`·`강제 취소` footer가 모두 `42px` 높이·풀폭으로 정렬되고, `준비 시작` 진입 후 `준비 시작 확인`·`취소`도 같은 `42px` 높이로 이어짐을 확인했다. API 호출 없이 폼을 닫았다.
- 운영 seller `RESET-reset-order-parcel` 상세에서 `택배 발송 완료`·`강제 취소` footer가 모두 `42px` 높이·풀폭으로 정렬됨을 확인했다.
- 같은 택배 주문의 배송 정보에 `수거 예정 시각 Invalid Date`가 노출되는 `VF-007`을 발견했다.
- 원인은 목록 훅과 달리 상세 훅이 Firestore `Timestamp`를 ISO 문자열로 정규화하지 않은 점이다. `useOrderDetail` 조회 경계에서 `createdAt`, `updatedAt`, `requestedDeliveryDate`, `preparedAt`을 ISO 문자열로 정규화하도록 로컬 수정했다.
- 검증: 변경 seller 파일 Biome 0, seller 타입체크 0, seller 빌드 0. 배포와 운영 읽기 전용 재검증은 남아 있다.

## 14. 2026-05-31 VF-007 운영 종결

- 사용자 승인 후 seller 운영 배포 `dpl_3uKynyzevpMuLXZvdfNuBpswmZGs`를 실행했다.
- 배포는 `READY`이며 `https://seller.greenlove.co.kr` 별칭 연결을 확인했다.
- Chrome `정연` 프로필의 `https://seller.greenlove.co.kr/orders/reset-order-parcel` 상세를 새로고침했다.
- 배송 정보의 `수거 예정 시각`이 `5월 23일 오후 05:31`로 표시되고 `Invalid Date`가 사라진 것을 확인했다.
- `VF-007`을 종결했다. 운영 쓰기 API는 호출하지 않았다.

## 9. 2026-05-31 VF-004·VF-005 배포와 재검증

- 사용자 승인 후 소비자 최신 미승격 배포를 운영으로 승격했다. 배포 `dpl_93VjrxXyf11ZkBJupJ8t3eziYQve`는 `READY`, `target: production`.
- 소비자 운영 `https://greenlove.co.kr`에서 데스크톱 `1280x720`, 모바일 `375x812` 기본 배너 1장 렌더 복구를 확인했다. VF-005는 종결했다.
- 기본 배너 헤드라인과 이미지 겹침은 데스크톱과 모바일에서 계속 보인다. VF-001은 미완료로 유지한다.
- VF-004 수정분 API 배포 `8faa96e8-6186-4889-a03f-a279be2dd697`는 `SUCCESS`.
- VF-004 수정분 seller 배포 `dpl_CQinDk7rjZMZkjaMagY1p7doeUW1`은 `READY`, `seller.greenlove.co.kr` 별칭 연결 완료.
- 배포 후 정연 프로필의 seller 로그인 세션이 만료되어 `/onboarding`이 `/login`으로 이동했다. VF-004 완료 판정은 하지 않았다.

## 5. 다음 권장 순서

1. 판매자 주문 `/orders` 읽기 전용 잔여: 우선 알림 이동, 빈 상태, 체크박스 노출 범위, 필터 변경 시 선택 초기화
2. 판매자 정산 `/settlements` 읽기 전용 잔여: 상태 탭 필터, 전체 복귀, 빈 결과
3. 소비자 홈 `https://greenlove.co.kr` 공개 배너 읽기 전용 잔여
4. 모바일 `375px` 검증은 가능한 브라우저 뷰포트 제어가 확보되면 묶어서 진행
5. 운영 쓰기 항목은 테스트 데이터 또는 사용자 승인을 받은 건만 별도 진행

## 6. 작업 트리 주의

- 현재 작업 트리는 기존 사용자 변경과 이번 문서 변경이 함께 있어 깨끗하지 않다.
- unrelated 변경을 되돌리지 않는다.
- `.codex/`, `AGENTS.md`는 사용자 또는 환경 산출물 가능성이 있어 건드리지 않는다.

## 15. 2026-05-31 판매자 단건 택배 발송 읽기 전용 검증

- Chrome `정연` 프로필의 `https://seller.greenlove.co.kr/orders/reset-order-parcel` 상세에서 `택배 발송 완료` 모달을 열었다.
- 택배사 Select, 운송장번호 입력, 취소, 발송 완료 버튼이 한 모달 안에 표시됨을 확인했다.
- 운송장번호 빈 값과 `12` 입력에서는 발송 완료가 비활성이고, `123` 입력에서는 활성으로 바뀜을 확인한 뒤 값을 초기화했다.
- 택배사 `기타` 선택 시 `택배사 직접 입력` 필드가 나타나고, 직접 입력이 비어 있으면 발송 완료가 비활성임을 확인했다.
- 모달을 취소로 닫았으며 운영 쓰기 API는 호출하지 않았다.
- 다음 진입점은 모바일 폭 제어가 가능한 브라우저에서 판매자 정산 `#5~#7`, 주문 모바일 배치를 확인하거나, 현재 Chrome 세션에서 관리자 데스크톱 읽기 전용 항목을 이어가는 것이다.

## 16. 2026-05-31 공동구매 알림 잔여와 어드민 배너 검증

- Chrome 확장 브라우저 목록에서 `profileName === '정연'` 프로필을 선택하고 기존 운영 `/orders` 탭을 이어받았다.
- 일반 주문에서는 배너 `처리 필요 1건 · 지연 3건`, 공동구매에서는 `처리 필요 1건 · 지연 1건`을 확인했다.
- 공동구매 최초 목록은 비어 있었지만 `지연 주문 보기` 클릭 후 `🔴 지연 · 1건`, `RESET-reset-order-group` 1건이 표시되어 원본 `#158`, `#161`을 통과 처리했다.
- `/admin/banner`의 `새 배너` Drawer에서 CTA 한쪽 입력 시 `버튼 문구와 URL은 둘 다 입력하거나 둘 다 비워주세요.`, 기간 누락 시 `기간 배너는 시작일과 종료일이 필요합니다.` 빨간 알림과 입력 오류를 확인했다.
- 테스트 입력은 ESC로 Drawer를 닫아 폐기했다. 목록 재조회에서 기본 배너 1건만 유지됨을 확인했고 운영 데이터 저장은 발생하지 않았다.
- `#249`의 종료일 < 시작일과 `#250`의 CTA 양쪽 입력 통과는 잔여다. 다음 진입점은 해당 두 입력 검증 또는 모바일 폭 제어가 가능한 브라우저의 `375px` 묶음이다.
- 앱 내 브라우저는 `375x812` 뷰포트 제어가 가능하다. 공개 홈 `https://greenlove.co.kr`에서 배너 렌더, 콘솔 오류 0건, `scrollWidth 360 === clientWidth 360`을 확인했다.
- 앱 내 브라우저에는 판매자 로그인 세션이 없다. 인증이 필요한 seller·admin 모바일 묶음은 Chrome `정연` 프로필의 폭 제어 수단을 확보하거나 별도 인증 가능한 테스트 브라우저에서 진행한다.

## 17. 2026-05-31 어드민 배너 읽기 전용 잔여 검증

- Chrome `정연` 프로필의 운영 `/admin/banner` 탭을 이어받아 기본 배너 1건 유지를 확인했다.
- `새 배너` Drawer에서 `2026-05-31 → 2026-05-30` 역전 기간을 입력하고 `종료일은 시작일보다 빠를 수 없습니다.` 빨간 알림과 입력 오류를 확인했다.
- CTA 버튼1 문구와 URL을 모두 채운 상태에서는 CTA 오류가 사라지고 남겨 둔 역전 기간 오류만 유지되어 CTA 양쪽 입력 통과를 확인했다.
- 기본 배너 수정 Drawer에서 기존 이미지 미리보기와 태그·헤드라인·서브텍스트·CTA 임시 입력의 즉시 반영을 확인했다.
- 모든 임시 입력은 ESC로 폐기했다. 목록은 원래 헤드라인의 기본 배너 1건으로 유지됐고 운영 저장 API는 호출하지 않았다.
- `pending-visual-verify-20260529.md` §28 `#249~#251`을 통과 처리했다. 다음 진입점은 운영 쓰기 승인이 필요한 `#252~#253` 또는 인증 가능한 모바일 브라우저의 `#254`다.

## 18. 2026-05-31 어드민 주문·드라이버 데스크톱 읽기 전용 검증

- Chrome `정연` 프로필의 로그인 세션을 이어받았다. 확장 세션의 현재 화면은 `1920px` 이상이며 `375px` 폭 강제 제어가 노출되지 않아 인증 필요 모바일 묶음은 계속 보류한다.
- 운영 `/admin/orders`에서 데스크톱 테이블 `주문ID·스토어·상태·송장·금액` 열, 송장 미보유 주문의 `-`, 상태 Badge·금액·강제환불 버튼 공존을 확인했다.
- `RESET-reset-order-parcel 상세 보기`로 읽기 전용 `주문 상세` 모달을 열어 주문·구매자·배송·송장·생성 정보와 송장 `-` 표시를 확인했다. ESC로 닫은 뒤 목록과 필터 UI가 유지됐다.
- 운영 `/admin/orders` 최초 정렬 Select `최신순`, 페이지 크기 `50개`를 확인했다. 실제 환불·취소 API는 호출하지 않았다.
- `/admin/orders`에서 `오래된순`, 페이지 크기 `25개·100개·50개`, `준비중` 필터 3건, 해당 조합의 수동 새로고침 유지까지 확인했다. 전체 주문이 10건이라 `더 보기`는 노출되지 않아 `#185`는 잔여다.
- 운영 `/admin/drivers`에서 `전체 → 승인 대기 → 승인 완료 → 정지됨` 라벨 순서와 각 탭 재조회, 검색 입력·새로고침 유지, 빈 상태를 확인했다. 승인 대기 카드의 승인·정지 버튼 노출은 확인했으나 확인창과 상태 변경은 잔여다.
- `pending-visual-verify-20260529.md` `#150`, `#153`, `#175~#177`, `#180`, `#182~#184`, `#186~#187`, `#231~#232`를 통과 처리했다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음 또는 운영 쓰기 승인·충분한 테스트 데이터가 필요한 잔여다.

## 19. 2026-05-31 어드민 정산 데스크톱 읽기 전용 검증

- 운영 `/admin/settlements`에서 상태 탭 `전체 → 정산 대기 → 확정 → 지급 완료 → 취소`와 상태별 `5 → 0 → 3 → 1 → 1`건, 전체 5건 복귀를 확인했다.
- confirmed 1건 선택 후 지급 완료 탭으로 이동하면 선택이 0건으로 정리됐다. 전체 선택은 confirmed 3건만 잡아 `선택 3건 · 지급 합계 99,500원`으로 표시됐고 `선택 해제`로 0건 복귀했다.
- 빠른 기간은 `이번 주 2026-05-25 → 2026-05-31`, `이번 달 2026-05-01 → 2026-05-31`, `지난달 2026-04-01 → 2026-04-30`으로 조회됐다. 시작일·종료일 clear 후 전체 목록으로 복귀했다.
- `이번 달 + 확정 + 80189070-...` 복합 필터에서 desc 순서 2건이 오류 없이 조회됐다. 필터 입력과 날짜를 모두 비워 전체 5건 초기 상태로 돌려놓았다.
- 실제 단건·일괄 지급 요청은 호출하지 않았다. `pending-visual-verify.md` `#81~#83`, `#95~#100`, `#102~#104`, `#106~#107`을 통과 처리했다.
- 다음 진입점은 직접 DatePicker 선택 `#105`, 인증 가능한 모바일 묶음, 또는 운영 쓰기 승인 필요 항목이다.

## 20. 2026-05-31 정산 DatePicker·초대 검색 데스크톱 읽기 전용 검증

- Chrome `정연` 프로필의 운영 `/admin/settlements` 탭을 이어받아 시작일·종료일 DatePicker를 각각 `2026-05-22`로 직접 선택했다.
- 정산 목록은 전체 5건에서 시작일 선택 후 3건, 종료일 선택 후 2건으로 재조회됐고 합계도 함께 갱신됐다. 이후 `이번 달` 버튼으로 `2026-05-01 → 2026-05-31` 5건 상태를 복구했다.
- 운영 `/admin/invite`에는 발급 토큰이 0건이었다. 검색 입력 노출, `abc` 입력 시 기본 빈 상태 유지, `abcd` 입력 시 `일치하는 토큰이 없습니다.`, 검색어 제거 후 기본 빈 상태 복귀를 확인했다.
- 운영 토큰이 없어 실제 일치 prefix 결과 `#202`, 행별 복사·취소 버튼 회귀, 모바일 묶음은 계속 잔여다.
- `pending-visual-verify.md` `#105`, `pending-visual-verify-20260529.md` `#201`, `#203~#205`를 통과 처리했다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음 또는 운영 쓰기 승인·충분한 테스트 데이터가 필요한 잔여다.

## 21. 2026-05-31 셀러 주문 카드→상세 버튼 크기 읽기 전용 검증

- 운영 `/orders`에서 `직접 입력` 기간 필터로 `RESET-reset-order-faraway` 카드가 노출되는 상태를 확인했다.
- 목록 카드의 `준비 시작` 버튼은 높이 `36px`, `/orders/reset-order-faraway` 상세 footer의 `준비 시작`과 `강제 취소` 버튼은 높이 `42px`였다.
- 목록→상세의 버튼 높이 차이가 `6px`로 제한되어 과도한 크기 점프가 없음을 확인했다. 준비 시작 확인 동작과 운영 쓰기 API는 호출하지 않았다.
- `pending-visual-verify.md` `#18~#20`을 통과 처리했다.

## 22. 2026-05-31 어드민 판매자 목록 읽기 전용 검증과 VF-008·VF-009

- Chrome `정연` 프로필의 운영 `/admin/stores`에서 활성 판매자 3건, 검색 `난플렉스` 즉시 반영, `keyword` URL 반영, 새로고침 복원, `정리됨` 상태 필터 URL 반영, 빈 결과와 필터 초기화, 데스크톱 수수료율 정렬 헤더 전환을 확인했다.
- 난플렉스 수수료 편집에서 입력·저장·취소 배치와 focus ring 비충돌, `-0.1`, `1.5` 직접 입력 차단을 확인했다. 저장 API는 호출하지 않고 취소했다.
- 활성 판매자 `9b2cb652…`의 상호가 깨진 문자열로 표시되는 운영 데이터 문제를 `VF-008`로 기록했다.
- 수수료 `NumberInput` DOM에는 증감 버튼 2개와 `inputmode="decimal"`이 있지만 화면에서 제어가 보이지 않았다. seller 선택 CSS에 `NumberInput.css`가 빠진 원인을 확정하고 로컬 보완해 `VF-009`로 기록했다.
- 변경 CSS Biome 0, seller 타입체크 0, seller 빌드 0을 확인했다.
- 이 시점의 다음 진입점은 `VF-009` 운영 배포 승인 또는 인증 가능한 모바일 브라우저의 `375px` 묶음이었다. 운영 데이터 수정은 실행하지 않았다.

## 23. 2026-05-31 VF-009 운영 종결

- 사용자 승인 후 seller 운영 배포 `dpl_G5uNkgqeE7rgQkSCfYaFhwoWVacn`를 실행했다.
- 배포는 `READY`, `target: production`이며 `https://seller.greenlove.co.kr` 별칭 연결을 확인했다.
- Chrome `정연` 프로필의 운영 `/admin/stores`에서 난플렉스 수수료 편집을 열었다.
- `NumberInput` 증감 제어 2개가 입력칸 오른쪽에 표시되고, 위 제어 클릭 시 `0 → 0.01`, 아래 제어 클릭 시 하한 `0` 유지가 동작함을 확인했다.
- 취소로 편집 상태를 닫았으며 저장 API는 호출하지 않았다. `VF-009`를 종결했다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음 또는 사용자 승인 후 `VF-008` 운영 데이터 정리다.

## 24. 2026-05-31 관리자 확인창 읽기 전용 후속 검증

- Chrome `정연` 프로필의 운영 `/admin/drivers`에서 승인 대기 카드의 `승인`, `정지` 확인창과 문구를 확인하고 취소로 닫았다. 승인 완료·정지됨 탭은 운영 데이터가 없어 빈 상태만 확인했다. 상태 변경 API는 호출하지 않았다.
- 운영 `/admin/settlements`에서 confirmed 1건을 선택해 `선택한 1건`, `14,000원` 지급 합계를 다시 보여주는 일괄 지급 확인창을 확인했다. 취소 후 선택 해제로 원복했고 지급 API는 호출하지 않았다.
- 운영 `/admin/orders`에서 `RESET-reset-order-done`의 위험 단계 강제환불 모달에 빨간 경고, 필수 사유 입력, 빈 값 `환불 처리` 비활성을 확인했다. `RESET-reset-order-faraway` 일반 단계 모달은 선택 사유와 활성 `환불 처리`를 확인했다. ESC와 취소로 닫았고 환불 API는 호출하지 않았다.
- `pending-visual-verify.md` `#84`를 통과 처리하고, 세션δ 계약으로 대체된 `#118`은 `[-]`로 정리했다. `#140`은 외부 클릭 닫기만 잔여다. 드라이버 `#234`는 승인 완료·정지됨 카드 액션 확인이 남아 부분 완료다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음, 드라이버 승인 완료·정지됨 카드 액션, 또는 사용자 승인 후 `VF-008` 운영 데이터 정리다.

## 25. 2026-05-31 드라이버 검색·로딩 읽기 전용 후속 검증

- Chrome `정연` 프로필의 운영 `/admin/drivers` 승인 대기 탭에서 이메일 일부 `1778944207639` 검색 시 일치 카드 1건만 남고, 불일치 검색어에서는 `검색 결과가 없습니다.`, 검색어 제거 후에는 전체 목록 복귀를 확인했다.
- 승인 완료·정지됨 탭은 운영 데이터가 없어 `드라이버가 없습니다.` 빈 상태를 확인했다. 검색 결과 없음과 원본 목록 없음 문구가 구분된다.
- 이메일 검색어를 유지한 채 새로고침 아이콘을 눌러 `불러오는 중...`과 `새로고침` 표시, 완료 후 동일 1건 복귀를 확인했다. 승인 완료 탭 전환 로딩 중에도 검색 입력값과 4개 탭이 유지됐다.
- `pending-visual-verify.md` `#217`, `#222~#225`를 통과 처리했다. 상태 변경 API는 호출하지 않았다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음, 승인 완료·정지됨 드라이버 테스트 데이터 확보 후 액션 검증, 또는 사용자 승인 후 `VF-008` 운영 데이터 정리다.

## 26. 2026-05-31 어드민 주문 환불 모달 외부 클릭 종결

- Chrome `정연` 프로필의 운영 `/admin/orders`에서 `RESET-reset-order-done` 위험 단계 강제환불 모달을 열었다.
- 모달 바깥을 실제 좌표로 클릭해 모달이 닫히고 목록 화면으로 복귀함을 확인했다. 환불 API는 호출하지 않았다.
- 기존 ESC 닫기와 일반 단계 취소 닫기 결과를 합쳐 `pending-visual-verify.md` `#140`을 통과 처리했다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음, 승인 완료·정지됨 드라이버 테스트 데이터 확보 후 액션 검증, 또는 사용자 승인 후 `VF-008` 운영 데이터 정리다.

## 27. 2026-06-01 어드민 정산 후속과 주문 상태 필터 VF-010

- Chrome `정연` 프로필의 운영 `/admin/settlements`에서 플랫폼 수수료 합계 카드 hover 시 `현재 필터 범위의 confirmed+paid 합계이며 pending·cancelled는 제외합니다.` 툴팁을 확인했다.
- 같은 화면의 새로고침 클릭 직후 버튼 비활성·`불러오는 중...` 표시와 완료 후 5건 복귀를 확인했다. `pending-visual-verify.md` `#93~#94`를 통과 처리했다.
- 운영 `/admin/orders`의 상태 Select는 전체+11개 옵션 라벨·순서를 정상 표시하지만, `취소됨` 선택 후에도 전체 10건이 남았다. 난플렉스 스토어 필터는 7건으로 정상 축소되지만 이어 `준비중`을 선택해도 7건 전체가 유지됐다.
- 로컬 프론트와 API 코드는 상태 쿼리를 전달·적용하고, `firestore.indexes.json`에도 주문 상태 조합 인덱스가 정의돼 있다. 운영 API 상태 조회 실패 후 프론트가 이전 목록을 유지하는 것으로 추정한다.
- Railway CLI OAuth가 만료되어 운영 로그 확인은 실패했다. `railway login` 재인증 없이 운영 로그·배포·데이터 변경은 진행하지 않았다.
- 신규 보완 작업 `VF-010`으로 기록했다. 다음 진입점은 Railway 읽기 전용 로그 또는 인증된 API 응답으로 상태 조회 실패 원인을 확정하는 것이다.
- 셀러 주문 상세 읽기 전용 잔여도 일부 확인했다. `RESET-reset-order-faraway` 접수 주문은 `강제 취소` 확인창에서 4자 `테스트` 입력을 `최소 5자 이상 입력해주세요`로 차단했고, `RESET-reset-order-done` 배달완료 주문은 강제 취소 버튼 없이 반품 신청 안내만 표시했다. 모달은 닫기로 폐기했고 운영 쓰기 API는 호출하지 않았다.
- `pending-visual-verify.md` `#121~#122`는 확인한 상태 범위를 메모하고, 확정 및 나머지 위험 상태 대조가 남아 있어 `[ ]`를 유지했다. 브라우저는 `https://seller.greenlove.co.kr/orders`로 복귀했다.

## 28. 2026-06-01 VF-010 프론트 오인 방지 로컬 수정

- `useAdminOrders`가 첫 페이지 조회 실패 시 기존 `orders`와 `nextCursor`를 비우도록 보완했다. `더 보기` 실패는 이미 읽은 목록을 유지한다.
- `/admin/orders` 필터 아래에 빨간 오류 안내를 추가해 조회 실패를 정상 필터 결과처럼 오인하지 않게 했다.
- `admin-orders.spec.ts`에 상태 조회 실패 fixture와 이전 4개 행 제거·오류 안내 노출 회귀를 추가했다.
- 검증: 변경 파일 Biome 0, seller 타입체크 0, seller 빌드 0, e2e chromium·mobile 22건 수집 확인.
- e2e 실행은 두 환경에서 차단됐다. 운영 URL은 현재 배포본에 로컬 최신 `스토어` Select가 없어 기존 케이스부터 실패했고, 로컬 서버는 `AUTH_SECRET`·Firebase 공개 키 부재로 `/api/auth/csrf` 500을 반환했다.
- Railway CLI도 OAuth 만료 상태다. 다음 진입점은 사용자 재인증 후 Railway 읽기 전용 로그로 상태 조회 실패 원인을 확정하고, 필요한 운영 인덱스 배포와 seller 배포 승인을 받은 뒤 `#117`, `#132`, 오류 안내를 운영에서 재검증하는 것이다.

## 29. 2026-06-01 판매자 주문 일괄 준비 취소 경로 종결

- Chrome `정연` 프로필의 운영 `/orders`에서 `직접 입력` 날짜 범위로 `RESET-reset-order-faraway` 결제 완료 카드를 노출했다.
- 카드의 일괄 준비 체크박스를 선택하자 액션 바가 `1건 선택`으로 갱신되고 `준비 시작` 버튼이 활성화됐다.
- 액션 바의 `준비 시작`을 눌러 `선택한 주문을 준비 중으로 바꿀까요?`, `1건의 주문을 한 번에 준비 중으로 변경합니다.` 확인창을 확인했다.
- 확인창은 `취소`로 닫았고 카드 상태가 `결제 완료`로 유지됨을 확인했다. 구현도 `ConfirmModal.onClose`가 `setBulkConfirmOpened(false)`만 수행하고 PATCH는 `onConfirm={handleBulkPrepare}`에서만 실행함을 대조했다.
- `pending-visual-verify-20260529.md` `#162`, `#165`를 통과 처리했다. 운영 쓰기 API는 호출하지 않았다.
- Chrome 확장 세션은 뷰포트 강제 기능이 없어 인증 필요 `375px` 모바일 묶음은 계속 보류한다. 다음 읽기 전용 후보는 데스크톱에서 데이터 조건이 갖춰진 잔여 항목이며, 운영 데이터 변경이 필요한 `VF-008`은 사용자 승인 전까지 수행하지 않는다.

## 30. 2026-06-01 스토어 필터 복원과 셀러 취소 상세 후속

- Chrome `정연` 프로필의 운영 `/admin/stores`에서 `난플렉스` 검색과 `정리됨` 상태를 함께 적용해 URL `?keyword=난플렉스&status=archived` 반영을 확인했다.
- 어드민 주문 화면을 다녀온 뒤 브라우저 뒤로가기를 실행하자 URL, 검색 입력, `정리됨` Select와 조건 불일치 빈 결과가 복원됐다. `필터 초기화` 후 `/admin/stores` 기본 URL 복귀도 확인했다. `pending-visual-verify.md` `#60`은 모바일 Select만 잔여다.
- 운영 `/orders/reset-order-cancelled` 상세에서 `취소 사유 검증용 취소` 표시를 확인했다. `/orders/reset-order-group` 상세에서는 공동구매 `결제 완료`, 수량 `1 / 20개 (최소 5개)`, `준비 시작`, `강제 취소` 액션을 확인했다.
- 운영 쓰기 API는 호출하지 않았다. 다음 읽기 전용 후보는 데스크톱 데이터 조건이 갖춰진 잔여이며, 인증 필요 모바일 `375px` 묶음과 `VF-008` 운영 데이터 정리는 계속 보류한다.

## 31. 2026-06-01 드라이버 가입일과 승인 대기 액션 후속

- Chrome `정연` 프로필의 운영 `/admin/drivers` 승인 대기 탭에서 각 카드 이메일 아래 `가입일 2026-05-17`, `가입일 2026-05-16` 형식의 KST 날짜 표시를 확인해 `pending-visual-verify.md` `#220`을 통과 처리했다.
- 승인 대기 카드의 초록 기본 `승인`, 빨강 light `정지` 버튼 노출과 카드 내 정렬을 확인했다.
- 승인 완료·정지됨 탭은 운영 데이터가 없어 해당 카드의 `정지`, `정지 해제` 노출·스타일·확인창은 잔여다. 인증 필요 모바일 `375px` 배치도 계속 보류한다.
- 운영 쓰기 API는 호출하지 않았다.
## 32. 2026-06-01 드라이버 테스트 데이터와 VF-008 원본 읽기 전용 확인

- Firebase Admin SDK로 운영 Firestore를 읽기 전용 조회했다. `users` 중 `role=driver`는 총 19건이며 `승인 대기 19건`, `승인 완료 0건`, `정지됨 0건`이다.
- 승인 완료·정지됨 카드의 액션 버튼과 확인창을 운영 화면에서 검증할 테스트 데이터는 현재 없다. 상태 변경 API는 호출하지 않았다.
- `VF-008` 대상은 활성 스토어 `9b2cb652-ff77-46b9-a773-e1efa78fb763`이며 Firestore 원본 `name` 자체가 깨진 문자열 `�׽�Ʈ �ɰ���`로 저장되어 있다. 화면 렌더 문제가 아니라 운영 저장 데이터 문제임을 확인했다.
- `VF-008` 상호 정리는 향후 작업으로 보류한다. 정상 상호명을 사용자에게 확인받고 운영 Firestore 수정에 대한 별도 승인을 받은 뒤 `name`만 정리한다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음이다.

## 33. 2026-06-01 소비자 관리 데스크톱 읽기 전용 후속

- Chrome `정연` 프로필의 운영 `/admin/users`에서 소비자 목록이 23건으로 조회됨을 확인했다.
- 가입일은 `2026-05-17 → 2026-04-06` 내림차순이며, 현재 23건의 전화는 모두 빈 값 표시 `-`다.
- 이름 `E2E Consumer`, 이메일 일부 `1778944207113` 검색은 각각 1건으로 축소되고, 불일치 검색은 `검색 결과가 없습니다.`를 표시했다.
- 상태 탭은 `전체 23건 → 정상 23건 → 정지 0건 → 전체 23건`으로 전환됐다.
- 정상 소비자 행의 `정지` 확인창에서 `계정 정지`, `이 계정을 정지하시겠습니까?`를 확인하고 취소로 닫았다. 운영 쓰기 API는 호출하지 않았다.
- 정지 소비자가 없어 복구 확인창, 실제 전화 데이터가 없어 전화 검색은 잔여다. 인증 필요 모바일 `375px` 묶음도 계속 보류한다.
- 소비자 ID 접두사 `69dcfab6…` 1건의 이름이 깨진 문자열로 표시돼 `VF-011` 운영 데이터 확인 후보로 기록했다.
- 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음이다. `VF-008`·`VF-011` 운영 데이터 정리는 별도 승인 전까지 보류한다.

## 34. 2026-06-01 VF-011 원본 읽기 전용 확인

- Firebase Admin SDK로 운영 Firestore를 읽기 전용 조회했다.
- 대상은 `users/69dcfab6-4dca-43c0-952d-908001257168`, 이메일 `admin@test.com`, 역할 `consumer`다.
- 원본 `name` 자체가 화면과 동일한 깨진 문자열 `���`로 저장되어 있다. 같은 `/admin/users` 목록의 다른 한글 이름은 정상 표시되므로 프론트 렌더링 문제가 아니다.
- `VF-011`은 운영 데이터 정리 항목으로 유지한다. 정상 이름을 사용자에게 확인받고 운영 Firestore 수정에 대한 별도 승인을 받은 뒤 `name`만 정리한다.
- 운영 데이터 수정은 수행하지 않았다. 다음 진입점은 인증 가능한 모바일 브라우저의 `375px` 묶음이다.

# 2026-05-29 추가 육안검증 묶음

> 분리 출처: `pending-visual-verify.md` 500라인 가드. 본 파일은 2026-05-29에 추가된 판매자 주문·어드민 주문·어드민 초대 검증 항목을 보관한다.

## §14. 판매자 주문 상세 택배 발송 운송장 입력

> 출처: `seller-orders-improve-plan.md` §A. 택배 주문의 `PREPARING → DELIVERED` 전환 전 택배사와 운송장번호를 입력하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 143 | 택배 발송 모달 | 판매자 주문 상세의 `택배 발송 완료` 클릭 시 택배사 Select, 운송장번호 입력, 취소, 발송 완료 버튼이 한 모달 안에 표시된다 | [x] | 2026-05-31 운영 `RESET-reset-order-parcel` 상세에서 모달 구성 확인. 실제 발송 요청 없음 |
| 144 | 필수값 차단 | 택배사 또는 운송장번호가 비어 있거나 운송장번호가 3자 미만이면 `발송 완료` 버튼이 비활성화된다 | [x] | 2026-05-31 운영에서 운송장번호 빈 값·`12` 입력 시 비활성, `123` 입력 시 활성 확인 후 초기화. 실제 발송 요청 없음 |
| 145 | 기타 택배사 | 택배사 `기타` 선택 시 직접 입력 필드가 표시되고, 직접 입력값이 없으면 발송 완료할 수 없다 | [x] | 2026-05-31 운영에서 `기타` 선택 후 `택배사 직접 입력` 필드 노출과 빈 값 차단 확인. 실제 발송 요청 없음 |
| 146 | 발송 완료 후 노출 | 정상 발송 완료 후 판매자 주문 상세에 택배사와 운송장번호가 표시되고 `택배 발송 완료` 버튼은 사라진다 | [x] | 2026-06-05 최신 seller 프리뷰에서 송장 보유 완료 주문 상세가 `CJ대한통운`, `1234567890`을 표시하고 `택배 발송 완료` 버튼이 사라짐을 확인했다. `seller-parcel-ship.spec.ts --grep "배송 완료 택배"` chromium·mobile 2/2 통과 |
| 147 | 소비자 주문 상세 노출 | 소비자 주문 상세에서 판매자가 저장한 택배사와 운송장번호를 확인할 수 있다 | [ ] | 2026-06-05 consumer 프리뷰에서 주문 상세 진입과 API 응답 `courierCompany=CJ대한통운`, `trackingNumber=1234567890`은 확인했으나 화면에 송장 행이 렌더되지 않아 보류. 로컬 소스에는 송장 행 조건이 있으므로 최신 consumer 배포 또는 로컬 최신 빌드 재검증 필요 2026-06-05 로컬 최신 코드에서 송장 필드가 있으면 `deliveryMethod` 불일치와 무관하게 택배사·운송장번호를 표시하도록 UI 조건을 보강하고 fixture 회귀 케이스를 추가했다. `pnpm --filter consumer build`는 통과했으나 dev 서버가 `/e2e/order-cancel-status` 컴파일에서 멈춰 Playwright fixture 육안검증은 계속 보류한다. |
| 148 | 모바일 배치 | 375px 폭에서 모달 제목, Select, 입력창, 버튼 영역이 겹치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-06-05 최신 seller 프리뷰에서 모바일 택배 발송 모달의 택배사 combobox, 운송장번호, 취소·발송 완료 버튼과 문서 가로 넘침 0을 확인했다. `seller-parcel-ship.spec.ts --grep "모바일 택배"` chromium·mobile 2/2 통과 |

## §15. 어드민 주문 목록 송장 표시

> 출처: `admin/admin-tab-orders-plan.md` B-8.7 A1. 셀러 택배 송장 필수화 이후 운영자가 `/admin/orders`에서 송장을 읽기 전용으로 확인하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 149 | 데스크톱 송장 컬럼 | `/admin/orders` 데스크톱 테이블에 `송장` 컬럼이 보이고, 송장 보유 주문은 택배사와 운송장번호가 함께 표시된다 | [x] | 2026-05-29 로컬 seller fixture e2e 18/18 통과 |
| 150 | 송장 빈 값 | 송장 정보가 없는 기존 주문·직접배송·거점배송 주문은 `송장` 칸이 `-`로 표시된다 | [x] | 2026-05-31 운영 `/admin/orders`에서 기존 주문과 `RESET-reset-order-parcel`을 포함한 송장 미보유 주문의 `송장` 칸이 `-`로 표시됨을 확인 |
| 151 | 모바일 송장 행 | 375px 폭 카드에서 스토어 ID 아래에 `송장` 행이 보이고 택배사·운송장번호가 카드 안에 수납된다 | [x] | 2026-06-02 `admin-orders.spec.ts` fixture에서 송장 보유·빈 값 카드의 스토어 ID 아래 송장 행과 카드 내부 수납 확인 |
| 152 | 모바일 긴 번호 | 긴 운송장번호가 카드 밖으로 넘치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-06-02 `admin-orders.spec.ts` fixture에 40자 운송장번호를 주입하고 송장 카드·문서 가로 넘침 0 확인 |
| 153 | 기존 액션 회귀 | 강제환불 버튼, 상태 Badge, 금액 표시가 송장 행 추가 후에도 기존 위치와 동작을 유지한다 | [x] | 2026-05-31 운영 데스크톱 테이블에서 상태 Badge·금액·강제환불 버튼이 송장 열과 함께 유지됨을 확인. 실제 환불 요청 없음 |
| 154 | 자동 e2e | `admin-orders.spec.ts`에서 송장 보유 주문의 택배사·운송장번호 표시 테스트가 통과한다 | [x] | 2026-05-29 `SELLER_BASE=http://localhost:3011 pnpm --filter e2e test -- admin-orders.spec.ts` 18/18 통과 |

## §16. 판매자 주문 우선 알림 배너

> 출처: `seller-orders-improve-plan.md` §B 1차. `/orders` 진입 시 처리 필요 주문과 지연 주문을 먼저 보여주는 화면 내 알림 배너다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 155 | 배너 노출 | 처리 필요 또는 지연 주문이 있으면 주문 목록 상단에 `먼저 확인할 주문이 있습니다` 배너가 표시된다 | [x] | 2026-05-31 `https://seller.greenlove.co.kr/orders` 첨부 화면에서 배너, 처리 필요 2건, 지연 3건, 이동 버튼 2개 노출 확인 |
| 156 | 처리 필요 이동 | `처리 필요 보기` 클릭 시 `처리 필요` 탭으로 이동하고 날짜 필터는 기본 `이번 주` 상태로 정리된다 | [x] | 2026-05-31 운영 `https://seller.greenlove.co.kr/orders`에서 `처리 필요 보기` 클릭 후 `처리 필요` 탭·`이번 주` 활성 확인 |
| 157 | 지연 주문 이동 | `지연 주문 보기` 클릭 시 지연 주문이 있는 탭으로 이동하고 `지연` 날짜 그룹이 목록 최상단에 표시된다 | [x] | 2026-05-31 운영에서 `지연 주문 보기` 클릭 후 `대기 중` 탭 이동·`🔴 지연 · 3건` 그룹 최상단 노출 확인 |
| 158 | 공동구매 지연 기준 | 공구 토글에서 `groupDeliveryDate`가 지난 주문은 배너의 지연 건수에 포함된다 | [x] | 2026-05-31 운영 `공동구매` 토글에서 배너 `처리 필요 1건 · 지연 1건` 노출 확인. `지연 주문 보기` 이동 후 `RESET-reset-order-group` 1건 표시 |
| 159 | 빈 상태 회귀 | 처리 필요와 지연 주문이 모두 없으면 배너가 보이지 않고 기존 empty state와 탭 배치가 유지된다 | [x] | 2026-06-03 환경 변수 게이트 하네스에서 실제 `OrderPriorityAlert` 빈 메타, 공통 탭 5개, 기존 empty state 유지를 검증 |
| 160 | 모바일 배치 | 375px 폭에서 배너 텍스트와 두 버튼이 카드 폭 안에 자연스럽게 줄바꿈되고 가로 스크롤을 만들지 않는다 | [x] | 2026-06-03 전용 fixture Playwright에서 알림 텍스트·두 버튼 노출과 문서 가로 넘침 0을 검증 |
| 161 | 공구 탭 밖 지연 주문 | 공구 토글에서 현재 탭 밖(`대기 중`·`배송 중`)의 지연 주문도 배너 지연 건수에 포함되고 `지연 주문 보기`가 해당 탭으로 이동한다 | [x] | 2026-05-31 운영 `공동구매` 최초 화면에서는 목록이 비어 있었으나 배너에 지연 1건이 포함됨을 확인. `지연 주문 보기` 클릭 후 `🔴 지연 · 1건`, `RESET-reset-order-group` 노출 |

## §17. 판매자 주문 일괄 준비 시작

> 출처: `seller-orders-improve-plan.md` §C 1차. 여러 주문 일괄 처리 중 송장번호와 충돌하지 않는 `준비 시작`만 먼저 처리하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 162 | 체크박스 노출 범위 | `ACCEPTED`·`CONFIRMED` 주문 카드에만 선택 체크박스가 보이고 `PREPARING` 이후 주문에는 보이지 않는다 | [x] | 2026-06-01 운영 `RESET-reset-order-faraway` 결제 완료 카드에서 일괄 준비 체크박스 노출을 확인했다. 상태 제한은 `canBulkPrepareOrder()` 단위 테스트의 `ACCEPTED·CONFIRMED=true`, `PREPARING·DELIVERING=false`와 운영 대기 중 탭의 택배 전용 체크박스 검증 `#168`을 함께 대조했다. |
| 163 | 액션 바 표시 | 준비 가능 주문이 있는 목록 상단에 준비 가능 건수, 선택 건수, 전체 선택, 선택 해제, `준비 시작` 버튼이 표시된다 | [x] | 2026-05-31 VF-002 seller 운영 배포 후 액션 바·카드 체크박스 내부 아이콘 정상 크기·위치 확인 |
| 164 | 선택 상태 정리 | 탭·판매 유형·날짜 필터 변경으로 대상 주문이 목록에서 사라지면 선택 상태도 함께 정리된다 | [x] | 2026-05-31 VF-006 seller 운영 배포 후 택배 1건 선택 → `대기 중` 탭 전환에서 선택 `0건`, 액션 비활성 복귀 확인. API 호출 없음 |
| 165 | 확인 모달 | 선택 후 `준비 시작` 클릭 시 선택 건수를 포함한 확인 모달이 열리고 취소하면 API 요청이 발생하지 않는다 | [x] | 2026-06-01 운영 `RESET-reset-order-faraway` 1건 선택 후 확인창의 `1건의 주문을 한 번에 준비 중으로 변경합니다.` 문구를 확인하고 취소했다. 카드 상태는 `결제 완료`로 유지됐다. 구현도 `ConfirmModal.onClose`가 `setBulkConfirmOpened(false)`만 수행하고 PATCH는 `onConfirm={handleBulkPrepare}`에서만 실행함을 대조했다. |
| 166 | 일괄 처리 결과 | 확인 시 선택 주문들이 `PREPARING`으로 변경되고, 성공 건은 선택 해제되며 성공·부분 실패 알림이 표시된다 | [x] | 2026-05-31 운영 화면에서 1건 처리 후 `준비 시작 완료`, `1건을 준비 중으로 변경했습니다.` 알림과 처리 필요 3→2건, 대기 중 2→3건 갱신 확인 |
| 167 | 모바일 배치 | 375px 폭에서 액션 바, 체크박스, 카드 내용, 확인 모달 버튼이 겹치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-06-03 `ENABLE_E2E_FIXTURES=true` 전용 `/e2e/order-bulk-prepare` 하네스에서 실제 `OrderBulkActionBar`·`OrderCard`·`ConfirmModal`을 조합하고 `375px` 액션 바·체크박스·카드 내용·확인 모달 버튼·문서 가로 넘침 0을 검증했다. 전용 Playwright chromium `1/1` 통과. |

## §18. 판매자 주문 일괄 택배 발송

> 출처: `seller-orders-improve-plan.md` §C 2차. `PREPARING` 택배 주문을 여러 건 선택하고, 주문별 송장 입력 후 배송 완료 처리하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 168 | 체크박스 노출 범위 | `대기 중` 탭에서 `PREPARING`·택배 주문 카드에만 선택 체크박스가 보이고 직배송·거점 주문에는 보이지 않는다 | [x] | 2026-05-31 운영 `대기 중` 탭에서 직배송 2건은 체크박스 미노출, 택배 1건만 `일괄 택배 발송 선택` 노출 확인 |
| 169 | 액션 바 표시 | 택배 발송 가능 주문이 있으면 목록 상단에 가능 건수, 선택 건수, 전체 선택, 선택 해제, `택배 발송` 버튼이 표시된다 | [x] | 2026-05-31 운영에서 `택배 발송 가능 1건`, 선택 수, 전체 선택, 선택 해제, `택배 발송` 노출 확인. 탭 이동 후 선택 `1건→0건` 초기화 확인 |
| 170 | 주문별 송장 모달 | 선택 후 `택배 발송` 클릭 시 선택 주문마다 택배사 Select와 운송장번호 입력창이 있는 모달이 열린다 | [x] | 2026-05-31 운영에서 택배 주문 1건 선택 후 `택배 일괄 발송` 모달·택배사·운송장번호 입력 노출 확인 |
| 171 | 기타 택배사 검증 | 주문별 택배사 `기타` 선택 시 직접 입력 필드가 보이고, 직접 입력값 없이는 발송할 수 없다 | [x] | 2026-05-31 운영 모달에서 `기타` 선택 후 `택배사 직접 입력` 필드 노출, 빈 값에서 완료 버튼 비활성 확인 |
| 172 | 필수값 차단 | 모든 선택 주문의 택배사와 3자 이상 운송장번호가 채워져야 `N건 발송 완료` 버튼이 활성화된다 | [x] | 2026-05-31 운영 모달에서 기본 택배사만 선택된 상태와 `기타` 직접 입력 빈 상태 모두 `1건 발송 완료` 비활성 확인 |
| 173 | 일괄 처리 결과 | 확인 시 성공 주문은 `DELIVERED`로 변경되고 선택 해제되며, 일부 실패 시 성공·실패 건수 알림이 표시된다 | [ ] | 기존 단건 API 반복 호출 |
| 174 | 모바일 배치 | 375px 폭에서 액션 바, 체크박스, 송장 모달 행, 버튼 영역이 겹치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-06-03 `ENABLE_E2E_FIXTURES=true` 전용 `/e2e/order-bulk-parcel-ship` 하네스에서 실제 `OrderBulkActionBar`·`OrderCard`·`BulkParcelShipModal`을 조합하고 `375px` 액션 바·체크박스·송장 모달 입력·버튼·문서 가로 넘침 0을 검증했다. 전용 Playwright chromium `1/1` 통과. |

## §19. 어드민 주문 상세 읽기 전용 모달

> 출처: `admin/admin-tab-orders-plan.md` B-9. F3 1차로 새 상세 API 없이 `/admin/orders` 목록 응답에 포함된 필드를 모달에서 읽기 전용으로 펼쳐 보여주는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 175 | 데스크톱 상세 버튼 | `/admin/orders` 데스크톱 테이블 각 행 우측에 상세 보기 아이콘이 보이고 클릭 시 `주문 상세` 모달이 열린다 | [x] | 2026-05-31 운영에서 `RESET-reset-order-parcel 상세 보기`를 눌러 `주문 상세` 모달 진입 확인 |
| 176 | 상세 필드 표시 | 모달에서 주문번호·상태·상품·수량·금액·구매자·배송지·송장·생성일이 읽기 전용으로 표시되고 없는 값은 `-`로 표시된다 | [x] | 2026-05-31 운영 `RESET-reset-order-parcel` 상세에서 주문번호·상태·상품·수량·금액·구매자·연락처·배송 방식·배송지·희망 배송일·준비 예정·송장·스토어 ID·사용자 ID·생성일·수정일 확인. 송장은 `-` |
| 177 | 환불 액션 공존 | 상세 보기 아이콘 추가 후에도 강제환불 버튼 노출·모달 동작이 기존과 동일하다 | [x] | 2026-05-31 운영 데스크톱 테이블에서 상세 보기와 강제환불 버튼 공존 확인. 실제 환불 요청 없음 |
| 178 | 모바일 상세 버튼 | 375px 폭 카드에서 `상세` 버튼이 금액·강제환불 버튼과 겹치지 않고 탭 가능하다 | [x] | 2026-06-01 fixture 인증 회귀에서 4개 카드 `상세` 버튼 노출, 가로 넘침 0, 탭 후 상세 모달 진입 확인 |
| 179 | 모바일 모달 배치 | 375px 폭에서 상세 모달의 라벨·값이 카드 폭 안에 줄바꿈되고 가로 스크롤을 만들지 않는다 | [x] | 2026-06-02 `admin-orders.spec.ts` fixture 인증 회귀에서 모바일 상세 모달 배송지 표시, 모달 자체와 문서 전체 가로 넘침 0을 검증 |
| 180 | 닫기 동작 | ESC·외부 클릭·닫기 버튼으로 상세 모달을 닫아도 목록 필터·자동 새로고침 상태가 유지된다 | [x] | 2026-05-31 운영 상세 모달을 ESC로 닫은 뒤 `/admin/orders`, 전체 주문 10건, 필터·자동 새로고침 UI 유지 확인 |
| 181 | 자동 e2e | `admin-orders.spec.ts`에서 상세 모달 노출과 핵심 필드 표시 테스트가 통과한다 | [x] | 2026-06-02 최신 seller 고정 프리뷰 `greenhub-seller-2omkcjr8s...`에서 모바일 모달 가로 넘침 단언 보강 후 chromium·mobile 28/28 통과. 운영 DB 쓰기 없음 |

## §20. 어드민 주문 정렬·커서 페이지네이션

> 출처: `admin/admin-tab-orders-plan.md` B-10. F5 1차로 `/admin/orders`의 200건 고정 조회를 `createdAt` 정렬과 cursor `더 보기` 흐름으로 바꾼다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 182 | 정렬 기본값 | 최초 진입 시 정렬 Select가 `최신순`이고 첫 페이지가 최신 주문부터 표시된다 | [x] | 2026-05-31 운영 `/admin/orders` 최초 진입에서 정렬 Select `최신순`, 페이지 크기 `50개` 확인 |
| 183 | 오래된순 전환 | 정렬을 `오래된순`으로 바꾸면 목록이 첫 페이지부터 다시 조회되고 오래된 주문부터 표시된다 | [x] | 2026-05-31 운영에서 `오래된순` 전환 후 `RESET-reset-order-cancelled`부터 시작하고 `20260101-000003`이 마지막인 역순 목록 확인 |
| 184 | 페이지 크기 전환 | 페이지 크기 25·50·100 변경 시 목록이 첫 페이지부터 다시 조회되고 버튼·카운트가 새 크기에 맞게 표시된다 | [x] | 2026-05-31 운영에서 `25개`, `100개`, 초기 `50개` 복귀를 순서대로 확인. 현재 전체 10건이라 각 선택에서 10건 노출 |
| 185 | 더 보기 | `더 보기` 클릭 시 기존 목록 아래에 다음 주문들이 이어 붙고 중복 주문이 보이지 않는다 | [x] | 2026-06-02 `admin-orders.spec.ts` 상태형 fixture에서 30건 중 25건 첫 조회 후 `cursor=25`로 5건을 이어 붙이고 `더 보기` 제거 확인 |
| 186 | 필터 공존 | 스토어·상태 필터 적용 상태에서 정렬·더 보기를 사용해도 필터 밖 주문이 섞이지 않는다 | [x] | 2026-05-31 운영에서 `오래된순`·`25개` 상태로 `준비중` 필터 적용 후 준비중 3건만 노출 확인. 스토어 필터와 `더 보기`는 데이터 조건 부족 |
| 187 | 새로고침 공존 | 수동 새로고침과 자동 새로고침은 현재 필터·정렬·페이지 크기를 유지하면서 첫 페이지를 다시 불러온다 | [x] | 2026-05-31 운영 `오래된순`·`25개`·`준비중` 조합에서 수동 새로고침 후 동일한 3건 유지 확인. 자동 새로고침 장기 대기는 생략 |
| 188 | 모바일 배치 | 375px 폭에서 스토어 Select, 상태 Select, 정렬 Select, 페이지 크기 Select, `더 보기` 버튼이 겹치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-06-02 `admin-orders.spec.ts` 상태형 fixture에서 4개 Select와 `더 보기` 노출, 문서 가로 넘침 0 확인 |

## §21. 어드민 초대 발급 내역 보기 개선

> 출처: `admin/admin-tab-invite-plan.md` 세션 S-A(T1~T3). 행별 복사, 발급일·사용일 표시, clipboard 폴백을 추가했다.
> 자동 검증은 통과했으나 로컬 dev 서버는 기존 환경 문제(`AUTH_SECRET` 부재, Firebase `auth/invalid-api-key`)로 `/admin/invite` 실제 화면 진입이 `/login` 이후 차단되어 브라우저 육안 확인은 운영/프리뷰에서 진행한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 189 | 데스크톱 행별 복사 | `/admin/invite` 데스크톱 테이블에서 모든 토큰 행에 복사 아이콘 버튼이 보이고, 클릭 시 해당 행 토큰만 복사됨 상태로 바뀐다 | [x] | 2026-06-02 fixture 인증 회귀에서 모든 토큰 행 복사 버튼, 해당 토큰 clipboard 기록, `복사됨` tooltip 확인 |
| 190 | 모바일 카드 복사 | 375px 폭 모바일 카드에서도 토큰 옆 복사 아이콘이 카드 폭 안에 수납되고 모든 상태 행에서 탭 가능하다 | [x] | 2026-06-01 fixture 인증 회귀에서 유효·사용됨·만료 3개 카드 복사 버튼 노출과 가로 넘침 0 확인 |
| 191 | 발급일·사용일 표시 | 데스크톱 테이블에 `발급일`, `사용일` 컬럼이 보이고 값은 `MM-DD HH:mm` KST 포맷, 미사용 토큰의 사용일은 `-`로 표시된다 | [x] | 2026-06-02 fixture 인증 회귀에서 `발급일`·`사용일`, `05-29 10:00`·`05-29 11:00`, 미사용 `-` 확인 |
| 192 | 모바일 카드 일시 표시 | 모바일 카드에 발급·사용·만료 정보가 세로로 표시되고 긴 토큰·Badge·복사 버튼과 겹치지 않는다 | [x] | 2026-06-01 fixture 인증 회귀에서 발급·사용·만료 정보, 긴 토큰, Badge, 액션과 가로 넘침 0 확인 |
| 193 | 발급 직후 복사 회귀 | 새 토큰 발급 직후 기존 복사 버튼도 정상 동작하며, 행별 복사 상태와 서로 잘못 연동되지 않는다 | [x] | 2026-06-02 fixture 인증 회귀에서 신규 토큰 발급·즉시 복사 후 기존 행 복사 시 신규 토큰 버튼 상태가 분리됨을 확인 |
| 194 | clipboard 폴백 | 브라우저 clipboard 권한 거부/비지원 상황에서 textarea 폴백을 시도하고, 폴백도 실패하면 빨간 notification이 표시된다 | [x] | 2026-06-02 fixture 인증 회귀에서 clipboard 거부 후 textarea 대체 복사, 대체 경로 실패 후 빨간 notification 확인 |

## §22. 어드민 초대 토큰 취소

> 출처: `admin/admin-tab-invite-plan.md` 세션 S-B(T4~T7). 유효 토큰 취소 엔드포인트, `취소됨` 상태, 확인창, reason별 실패 알림을 추가했다.
> 로컬 브라우저는 `http://localhost:3013/admin/invite` 진입 시 `/login`으로 리다이렉트되고, 기존 Firebase 공개 env 문제(`auth/invalid-api-key`)와 Auth.js 설정 오류로 인증 화면 이후 확인이 막혀 운영/프리뷰 육안 확인 대상으로 유지한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 195 | 데스크톱 취소 버튼 | `/admin/invite` 데스크톱 테이블에서 `유효` 토큰 행에만 취소 아이콘 버튼이 보이고 사용됨·만료·취소됨 행에는 보이지 않는다 | [x] | 2026-06-02 fixture 인증 회귀에서 유효 토큰만 취소 버튼 노출, 사용됨·만료 토큰 미노출 확인 |
| 196 | 취소 확인창 | 취소 버튼 클릭 시 토큰 전체 16자와 `취소 후에는 이 토큰으로 가입할 수 없습니다.` 문구가 확인창에 표시된다 | [x] | 2026-06-02 fixture 인증 회귀에서 전체 토큰과 가입 불가 문구 확인 후 취소 실행 |
| 197 | 취소 후 상태 | 확인 시 목록이 갱신되고 해당 토큰 상태가 `취소됨` orange 배지로 표시되며 새로고침 후에도 유지된다 | [x] | 2026-06-02 fixture 인증 회귀에서 취소 후 목록 재조회와 새로고침 뒤 `취소됨` 유지 확인. 색감 육안은 `#208` 유지 |
| 198 | 이중 가드 알림 | 이미 사용됨·이미 취소됨·만료 reason 응답은 각각 사용자에게 맞는 빨간 notification으로 표시된다 | [x] | 2026-06-02 fixture 인증 회귀에서 `already_used`·`already_revoked`·`expired` reason별 빨간 notification 확인 |
| 199 | 모바일 취소 버튼 | 375px 폭 카드에서 유효 토큰의 복사·취소 아이콘, 상태 배지, 발급·사용·만료 정보가 겹치지 않는다 | [x] | 2026-06-01 fixture 인증 회귀에서 유효 카드 복사·취소, 3개 상태 Badge, 일시 정보와 가로 넘침 0 확인 |
| 200 | 취소 토큰 가입 차단 | 취소된 토큰으로 판매자 가입을 시도하면 가입이 거부되고 토큰이 소비 처리되지 않는다 | [x] | 2026-06-02 API `auth.service.spec.ts`에서 사전 검증·트랜잭션 재검증 차단과 미소비 계약 재확인. 관리자 가드 포함 API 테스트 46/46 통과 |

## §23. 어드민 초대 토큰 prefix 검색

> 출처: `admin/admin-tab-invite-plan.md` 세션 S-C(T8'~T9'). 4자 이상 prefix 검색과 300ms debounce 입력을 추가했다.
> 로컬 인증 환경은 기존 Firebase 공개 env 문제로 화면 진입이 차단될 수 있어 운영/프리뷰에서 육안 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 201 | 검색 입력 표시 | `/admin/invite` 발급 내역 위에 토큰 검색 입력이 보이고 기존 발급·복사·취소 UI와 겹치지 않는다 | [x] | 2026-05-31 운영 `/admin/invite`에서 발급 내역 위 `토큰 4자 이상 검색` 입력 노출 확인. 운영 토큰은 0건 |
| 202 | 4자 prefix 검색 | 토큰 앞 4자 이상을 입력하면 해당 prefix와 일치하는 토큰만 목록에 표시된다 | [x] | 2026-06-02 fixture 인증 회귀에서 `INVITEVALID` 요청과 일치 토큰 1건·복사·취소 버튼 노출 확인 |
| 203 | 3자 이하 보호 | 1~3자 입력 상태에서는 prefix 필터 없이 기존 최신 50건 목록을 유지한다 | [x] | 2026-06-02 fixture 인증 회귀에서 `INV` 입력 후 debounce 대기에도 추가 조회가 발생하지 않음을 확인 |
| 204 | 빈 검색 복귀 | 검색어를 지우면 최신 50건 목록으로 복귀하고 기존 복사·취소 버튼이 정상 동작한다 | [x] | 2026-06-02 fixture 인증 회귀에서 빈 검색 재조회 후 유효·사용됨 토큰 복사 버튼 복귀 확인 |
| 205 | 검색 결과 없음 | 4자 이상 불일치 검색어에서는 `일치하는 토큰이 없습니다.` 빈 상태가 표시된다 | [x] | 2026-05-31 운영에서 `abcd` 입력 후 `일치하는 토큰이 없습니다.` 표시 확인 |
| 206 | 모바일 배치 | 375px 폭에서 검색 입력, 토큰 카드, 복사·취소 아이콘, 상태 배지가 겹치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-06-01 fixture 인증 회귀에서 검색 입력·토큰 카드·액션·Badge 노출과 가로 넘침 0 확인 |

## §24. 어드민 초대 취소 자동 검증 후 육안 확인

> 출처: `admin/admin-tab-invite-plan.md` 세션 S-D(T10~T12). 네트워크 fixture 기반 Playwright 스펙을 추가했다. 현재 운영 기본 실행은 아직 이전 UI를 보고, 로컬 실행은 Auth.js 설정 500으로 막혀 새 배포/로컬 인증 env 보정 후 자동 검증을 재실행한다. 실제 브라우저 육안 확인은 운영/프리뷰 인증 환경에서 최종 배치와 색상도 함께 확인한다.
> 2026-05-29 재검증: 로컬 seller env를 보정해 `SELLER_BASE=http://127.0.0.1:3016 pnpm --filter e2e test -- admin-invite-revoke.spec.ts`를 실행했고 chromium·mobile 합산 6/6 통과했다. 아래 항목은 자동 검증 통과 후에도 실제 운영/프리뷰 화면에서 색상·배치 감각을 확인하기 위한 육안 목록으로 유지한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 207 | 취소 확인창 배치 | `/admin/invite`에서 유효 토큰 취소 클릭 시 확인창 제목, 토큰 16자, 가입 불가 문구, 닫기·취소 버튼이 겹치지 않는다 | [x] | 2026-06-02 fixture 인증 회귀에서 확인창 제목·본문·확인 버튼 진입과 chromium·mobile 실행 통과 |
| 208 | 취소됨 배지 색상 | 취소 완료 후 `취소됨` 배지가 orange 계열로 보이고 유효·사용됨·만료 배지와 구분된다 | [ ] | T10 |
| 209 | reason 알림 위치 | `already_revoked` 등 409 reason 알림이 화면 주요 조작 영역을 가리지 않고 빨간 notification으로 표시된다 | [ ] | T11 |
| 210 | 검색과 취소 공존 | 4자 이상 prefix 검색 결과에서도 복사·취소 아이콘, 상태 배지, 발급·사용·만료 정보가 한 행/카드 안에서 깨지지 않는다 | [x] | 2026-06-02 fixture 인증 회귀에서 검색 결과 복사·취소 버튼과 chromium·mobile 가로 넘침 0 확인 |

## §25. 어드민 드라이버 탭 공통 SegmentedTabs 통일

> 출처: `admin/admin-tab-drivers-plan.md` 세션 S7(R2). 인라인 탭 렌더링을 공통 `SegmentedTabs`로 바꾸는 표현 레이어 정리다. 서버 필터, 검색, 새로고침, 승인·정지 액션 동작은 변경하지 않는다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 231 | 탭 라벨·순서 회귀 | `/admin/drivers`에서 `전체 → 승인 대기 → 승인 완료 → 정지됨` 순서와 문구가 기존과 동일하다 | [x] | 2026-05-31 운영 `/admin/drivers`에서 4개 탭 라벨과 순서 확인 |
| 232 | 탭 전환 동작 회귀 | 각 탭 클릭 시 기존처럼 현재 status 조건으로 재조회되고 검색 입력·새로고침 버튼이 유지된다 | [x] | 2026-05-31 운영에서 승인 대기·승인 완료·정지됨·전체를 순서대로 전환하고 로딩 후 결과 또는 빈 상태, `이름, 이메일 검색`, 새로고침 버튼 유지 확인 |
| 233 | 모바일 탭 배치 | 375px 폭에서 4개 탭이 줄바꿈·겹침 없이 가로 스크롤로 접근 가능하고 카드 영역과 충돌하지 않는다 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 4개 탭 동일 행 유지와 문서 가로 넘침 0 확인 |
| 234 | 액션 영역 회귀 | 공통 탭 교체 후에도 승인·정지·정지 해제 버튼과 확인창 표시가 기존과 동일하다 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 승인 대기·승인 완료·정지 카드의 액션 버튼과 승인·정지·해제 확인창 진입 확인 |

## §26. 어드민 드라이버 액션 결과 알림

> 출처: `admin/admin-tab-drivers-plan.md` 세션 S8(F6). status 서버 필터 때문에 액션 성공 후 카드가 현재 탭에서 사라질 수 있으므로, 성공 알림으로 다음 확인 위치를 안내한다. 카드 자동 이동 정책과 API 계약은 변경하지 않는다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 235 | 승인 성공 알림 | 승인 대기 카드 승인 성공 후 초록 알림이 보이고 `승인 완료 탭에서 확인할 수 있습니다.` 문구가 포함된다 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀 통과 |
| 236 | 정지 성공 알림 | 승인 완료 카드 정지 성공 후 초록 알림이 보이고 `정지됨 탭에서 확인할 수 있습니다.` 문구가 포함된다 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀 통과 |
| 237 | 정지 해제 성공 알림 | 정지 카드 정지 해제 성공 후 초록 알림이 보이고 `승인 완료 탭에서 확인할 수 있습니다.` 문구가 포함된다 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀 통과 |
| 238 | 실패 알림·모달 유지 | 액션 API 실패 시 빨간 실패 알림이 보이고 확인창은 닫히지 않아 재시도할 수 있다 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 500 실패 주입 후 빨간 알림과 열린 확인창 유지 확인 |
| 239 | 모바일 알림 배치 | 375px 폭에서 알림이 제목·검색·탭·확인창 주요 조작 영역을 과도하게 가리지 않는다 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 승인 확인창·성공 알림과 문서 가로 넘침 0 확인 |
## 짠27. 어드민 배너 다중 배너 API 기반

> 출처: `admin/admin-banner-multi-sdd.md` S4. 백엔드·데이터 모델 기반만 반영했으며, 어드민 목록 UI와 손님 캐러셀은 후속 S5/S6에서 육안 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 240 | 기존 단건 배너 호환 | 기존 `/admin/banner` 저장·조회와 공개 `/banner` 조회가 기본 배너 문서 `banners/main_hero`를 계속 사용한다 | [-] | 공개 `GET /banner`는 후속 계약에서 제거되어 2026-05-31 운영 `404`를 확인했다. 어드민 `/admin/banner` 호환은 유지 |
| 241 | 다중 배너 목록 API | 관리자 인증 상태에서 `/admin/banners`가 기본 배너와 기간 배너를 함께 반환하고 기본 배너를 구분할 수 있다 | [x] | 2026-06-01 운영 `/admin/banner`에서 인증 목록 조회와 기본 배너 `main_hero` 1건 구분 확인 |
| 242 | 기간 배너 생성 검증 | `kind=scheduled` 생성 시 시작일·종료일 누락, 종료일 < 시작일, CTA 비대칭 입력이 서버에서 차단된다 | [x] | 2026-06-01 API 배너 단위 테스트 재실행 6/6 통과 |
| 243 | 기본 배너 삭제 차단 | `DELETE /admin/banners/main_hero`가 422를 반환하고 기본 배너 문서를 삭제하지 않는다 | [x] | 2026-06-01 API 배너 단위 테스트 재실행 6/6 통과 |
| 244 | Storage 정리 | 기간 배너 삭제 또는 이미지 교체 시 Firebase Storage 기존 파일이 함께 정리된다 | [x] | 2026-06-01 기간 배너 삭제 시 Storage 이미지 정리 단위 테스트 포함 API 배너 테스트 6/6 통과 |
| 245 | 손님 활성 배너 조회 | `/banners/active`가 KST 오늘 기준 기간 배너 N건과 기본 배너 1건을 반환하고 기간 배너를 `createdAt desc`로 정렬한다 | [x] | 2026-05-31 Railway 운영 API 배포 `f422b2ba-4cad-4b4c-aea4-d86bd6641789` 후 `GET /banners/active` 200, `scheduled: []`, `default: main_hero` 확인 |

## §28. 어드민 배너 목록형 UI·라이브 미리보기

> 출처: `admin/admin-banner-multi-sdd.md` S5. `/admin/banner` 단건 폼을 기본/기간 배너 목록과 추가·수정 Drawer로 재구성했다. 손님 캐러셀은 후속 S6 범위라 여기서는 어드민 UI만 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 246 | 배너 목록 표시 | `/admin/banner` 진입 시 기본 배너와 기간 배너가 목록으로 보이고 각 행에 종류·상태 배지·헤드라인·기간이 표시된다 | [x] | 2026-05-31 운영 API 배포 후 `/admin/banner`에서 기본 배너 `main_hero` 1건, 종류·상태·헤드라인·상시 기본 배너 문구 확인 |
| 247 | 기본 배너 보호 | 기본 배너 행에는 수정 버튼만 있고 삭제 버튼은 보이지 않는다 | [x] | 2026-05-31 운영 기본 배너 행에 `수정`만 노출, 삭제 버튼 미노출 확인 |
| 248 | 새 배너 Drawer | `새 배너` 클릭 시 오른쪽 Drawer가 열리고 이미지·텍스트·노출 기간·CTA 입력과 라이브 미리보기가 함께 보인다 | [x] | 2026-05-31 운영 `/admin/banner`에서 저장 없이 Drawer 열기·닫기 확인. 이미지, 텍스트, 기간, CTA, 라이브 미리보기 노출 |
| 249 | 기간 입력 검증 | 기간 배너에서 시작일·종료일 누락 또는 종료일 < 시작일이면 저장 요청 없이 빨간 알림과 입력 오류가 표시된다 | [x] | 2026-05-31 운영 `새 배너` Drawer에서 기간 누락과 `2026-05-31 → 2026-05-30` 역전 입력 모두 빨간 알림·입력 오류 확인. 저장 요청 없음 |
| 250 | CTA 검증 회귀 | Drawer에서도 버튼 문구와 URL 중 하나만 입력하면 저장이 차단되고, 둘 다 비우거나 둘 다 채우면 통과한다 | [x] | 2026-05-31 운영 `새 배너` Drawer에서 버튼1 문구만 입력 시 차단 확인. 둘 다 비운 상태는 기간 오류로 진행되고, 둘 다 채운 상태에서는 CTA 오류 없이 남겨 둔 역전 기간 오류로 진행됨을 확인. 저장 요청 없음 |
| 251 | 라이브 미리보기 동기화 | 이미지, 태그, 헤드라인, 서브텍스트, CTA 입력값이 미리보기 패널에 즉시 반영되고 텍스트 겹침이 없다 | [x] | 2026-05-31 운영 기본 배너 수정 Drawer에서 기존 이미지 미리보기와 태그·헤드라인·서브텍스트·CTA 임시 입력의 즉시 반영 확인. ESC로 폐기, 저장 없음 |
| 252 | 수정 저장 | 기존 기간 배너 수정 후 저장하면 Drawer가 닫히고 목록의 헤드라인·기간·상태 배지가 갱신된다 | [x] | 2026-06-02 `seller-banner.spec.ts` 성공형 fixture에서 기간 배너 헤드라인 수정 저장 후 Drawer 닫힘, 성공 알림, 목록 갱신 확인 |
| 253 | 삭제 흐름 | 기간 배너 삭제 확인 후 목록에서 사라지고 성공 알림이 표시된다. 기본 배너 삭제는 UI에서 진입할 수 없다 | [x] | 2026-06-02 `seller-banner.spec.ts` 성공형 fixture에서 기간 배너 삭제 확인 후 목록 제거, 성공 알림, 기본 배너 유지와 삭제 버튼 미노출 확인 |
| 254 | 모바일 배치 | 375px 폭에서 목록 카드, 새로고침·새 배너 버튼, Drawer 입력, 라이브 미리보기가 겹침·가로 스크롤 없이 표시된다 | [x] | 2026-06-02 `seller-banner.spec.ts` fixture에서 새 배너 Drawer CTA 4개 입력 1열과 가로 넘침 0을 검증했다. `Drawer.css` 선택 import와 `size="min(780px, 100vw)"` 보완 후 최신 seller 프리뷰 chromium·mobile 10/10 통과 |

## §29. 소비자 배너 다중 캐러셀

> 출처: `admin/admin-banner-multi-sdd.md` S6. 손님 첫 화면이 `/banners/active` 응답을 사용해 기간 배너와 기본 배너를 함께 보여주는지 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 255 | 기본 배너 1장 정적 표시 | 기간 배너가 없고 기본 배너만 있을 때 첫 화면 배너가 1장으로 보이며 좌우 버튼과 점 인디케이터가 보이지 않는다 | [x] | 2026-05-31 소비자 최신 배포 운영 승격 후 `https://greenlove.co.kr` 데스크톱 `1280x720`, 모바일 `375x812`에서 기본 배너 1장 렌더 확인. 기간 배너가 없어 좌우 버튼과 점 인디케이터는 숨겨짐. VF-005 종결 |
| 256 | 기간 배너 + 기본 배너 순서 | 기간 배너가 여러 장이면 `createdAt desc` 순서로 먼저 보이고 기본 배너가 마지막 슬라이드로 들어간다 | [x] | 2026-06-02 환경 변수 게이트 하네스의 실제 `HeroBannerCarousel`에 기간 배너 2장+기본 배너 1장을 넣고 최신 기간 배너→이전 기간 배너→기본 배너 순서와 점 3개 확인 |
| 257 | 자동 전환 | 슬라이드가 2장 이상이면 약 5초 뒤 다음 배너로 전환된다 | [x] | 2026-06-02 `consumer-banner-carousel.spec.ts`에서 포인터를 배너 밖으로 옮긴 뒤 5.5초 후 1→2번째 점 전환 확인 |
| 258 | 사용자 조작 중지 | 좌우 버튼 또는 점 인디케이터를 누르면 자동 전환이 멈추고 선택한 배너가 유지된다 | [x] | 2026-06-02 `consumer-banner-carousel.spec.ts`에서 다음 버튼, 3번째 점 조작 후 5.5초 동안 3번째 선택 유지 확인 |
| 259 | hover/focus 정지 | 마우스를 배너 위에 올리거나 컨트롤에 포커스하면 자동 전환이 진행되지 않는다 | [x] | 2026-06-02 `consumer-banner-carousel.spec.ts`에서 hover 중 정지, 이탈 후 재개, 컨트롤 focus 중 정지 확인 |
| 260 | 모바일 배치 | 375px 폭에서 배너 이미지, 헤드라인, CTA, 좌우 버튼, 점 인디케이터가 겹치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-05-31 VF-001 consumer 운영 배포 후 데스크톱 `1280x720`, 모바일 `375x812`에서 텍스트·이미지 분리, 가로 넘침 0 확인 |

## §30. 어드민 배너 다중 배너 S7 최종 확인

> 출처: `admin/admin-banner-multi-sdd.md` S7. S4~S6 코드가 반영된 뒤 배포·마이그레이션·운영 데이터 기준으로 최종 확인한다. 자동 검증은 로컬/프리뷰 환경 제약이 있을 수 있어 결과를 메모에 남긴다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 261 | 배포 반영 | api·seller·consumer 최신 커밋이 각각 배포되어 `/admin/banners`, `/admin/banner`, 소비자 첫 화면이 같은 API 계약을 본다 | [x] | 2026-05-31 API 운영 `/banners/active` 200·`/banner` 404, seller `/admin/banner` 기본 배너 1건, consumer 홈 기본 배너 렌더 확인 |
| 262 | 마이그레이션 멱등성 | `banners/main_hero`에 `kind:'default'`, `id:'main_hero'`가 반영되어 있고 스크립트 재실행 시 데이터가 중복 생성되지 않는다 | [x] | 일회성 merge 스크립트 실행 후 삭제 완료. 운영 `/banners/active`와 `/admin/banner`에서 `main_hero` 기본 배너 1건 유지 확인 |
| 263 | 성공 기준 장면 | 같은 날 노출되는 기간 배너 1장과 기본 배너 1장이 있을 때 소비자 첫 화면에서 기간 배너 다음 기본 배너로 넘겨 볼 수 있다 | [x] | 2026-06-02 환경 변수 게이트 하네스에서 기간 배너 뒤 기본 배너를 좌우 버튼·점으로 이동하고 자동 전환도 확인 |
| 264 | 기간 경계 KST | 종료일이 오늘인 기간 배너는 오늘 종일 노출되고, 종료일이 어제인 기간 배너는 노출되지 않는다 | [x] | 2026-06-02 `BannerQueryService` 단위 테스트에서 KST 오늘 종료 배너 포함, 어제 종료 배너 제외, 활성 기간 배너 `createdAt desc` 정렬 확인. 배너 API 테스트 6/6 통과 |
| 265 | 기본 배너 회귀 | 기간 배너가 0장이어도 기본 배너 1장이 첫 화면에 정적으로 표시되고 빈 화면이 되지 않는다 | [x] | 2026-05-31 소비자 최신 배포 운영 승격 후 `/banners/active`의 `default: main_hero`와 소비자 홈 기본 배너 1장 렌더가 일치함. VF-005 종결 |

## §31. 어드민 배너 공개 조회 호환 정리

> 출처: `admin/admin-banner-multi-sdd.md` §11. 소비자 첫 화면 공개 조회를 `/banners/active`로 고정하고 구 `GET /banner` 및 일회성 마이그레이션 스크립트를 제거한 뒤 배포 상태에서 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 266 | 공개 배너 새 계약 유지 | 배포 후 소비자 첫 화면이 `/banners/active` 응답으로 기본 배너만/기간+기본 배너를 정상 표시한다 | [x] | 2026-05-31 VF-005 소비자 운영 승격 후 `/banners/active`의 `default: main_hero`와 홈 기본 배너 1장 렌더 일치 확인 |
| 267 | 구 공개 엔드포인트 제거 | 배포 후 `GET /banner`가 더 이상 공개 배너 계약으로 동작하지 않고, 운영 확인은 `/banners/active`만 사용한다 | [x] | 2026-05-31 운영 API 배포 후 `GET /banner` 404 `Cannot GET /banner` 확인 |

## §32. 어드민 배너 S7 후속 최신 배포 확인

> 출처: `admin/admin-banner-multi-sdd.md` §12. 구 공개 조회와 일회성 마이그레이션 스크립트 제거 커밋까지 Vercel 배포가 완료된 상태에서 최종 화면을 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 268 | 최신 후속 배포 반영 | 커밋 `0f5146a` 배포에서 소비자 첫 화면은 `/banners/active` 계약으로 배너를 표시하고, 어드민 `/admin/banner` 목록·수정 흐름은 S5 UI 그대로 동작한다 | [x] | 2026-05-31 VF-005 소비자 운영 승격 후 홈 기본 배너 렌더 복구. 로그인 어드민 `/admin/banner` 기본 배너 1건, 수정 전용 보호, `새 배너` Drawer 확인 |

## §33. 어드민 드라이버 상세 정보 보강

> 출처: `admin/admin-drivers-detail-info-plan.md`. 저장된 `users.phone`은 즉시 운영 확인 정보로 표시하고, 차량 정보는 현재 입력 경로가 없으므로 값이 있을 때만 표시한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 269 | 연락처 표시 | `/admin/drivers` 카드에 드라이버 전화번호가 이름·이메일 아래에 표시된다 | [x] | 2026-06-05 최신 seller 프리뷰 admin state로 375px 수동 Playwright fixture 검증. `연락처 010-1000-0001` 표시와 API `status=pending&sort=createdAt_desc&limit=100` 요청 확인 |
| 270 | 차량 정보 표시·미등록 | 차량 필드가 있으면 `차량 차종 · 차량번호`가 보이고, 없으면 `차량 정보 미등록`이 보인다 | [x] | 2026-06-05 같은 fixture에서 `차량 냉장 탑차 · 서울12가3456`과 승인 완료 카드의 `차량 정보 미등록` 표시 확인 |
| 271 | 전화번호 검색·모바일 배치 | 전화번호 일부로 검색 가능하고 375px 카드에서 연락처·차량·가입일·액션 버튼이 가로 넘침 없이 표시된다 | [x] | 2026-06-05 375px에서 전화번호 `010-1000-0002` 검색으로 승인 드라이버만 표시됨을 확인하고 pending·approved·검색 상태 모두 문서 가로 넘침 0 확인 |

# 2026-05-29 추가 육안검증 묶음

> 분리 출처: `pending-visual-verify.md` 500라인 가드. 본 파일은 2026-05-29에 추가된 판매자 주문·어드민 주문·어드민 초대 검증 항목을 보관한다.

## §14. 판매자 주문 상세 택배 발송 운송장 입력

> 출처: `seller-orders-improve-plan.md` §A. 택배 주문의 `PREPARING → DELIVERED` 전환 전 택배사와 운송장번호를 입력하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 143 | 택배 발송 모달 | 판매자 주문 상세의 `택배 발송 완료` 클릭 시 택배사 Select, 운송장번호 입력, 취소, 발송 완료 버튼이 한 모달 안에 표시된다 | [ ] | §A A-M1 |
| 144 | 필수값 차단 | 택배사 또는 운송장번호가 비어 있거나 운송장번호가 3자 미만이면 `발송 완료` 버튼이 비활성화된다 | [ ] | A-M3 |
| 145 | 기타 택배사 | 택배사 `기타` 선택 시 직접 입력 필드가 표시되고, 직접 입력값이 없으면 발송 완료할 수 없다 | [ ] | A-M2 |
| 146 | 발송 완료 후 노출 | 정상 발송 완료 후 판매자 주문 상세에 택배사와 운송장번호가 표시되고 `택배 발송 완료` 버튼은 사라진다 | [ ] | A-M4 |
| 147 | 소비자 주문 상세 노출 | 소비자 주문 상세에서 판매자가 저장한 택배사와 운송장번호를 확인할 수 있다 | [ ] | A.2 |
| 148 | 모바일 배치 | 375px 폭에서 모달 제목, Select, 입력창, 버튼 영역이 겹치거나 가로 스크롤을 만들지 않는다 | [ ] | 육안 검증 |

## §15. 어드민 주문 목록 송장 표시

> 출처: `admin/admin-tab-orders-plan.md` B-8.7 A1. 셀러 택배 송장 필수화 이후 운영자가 `/admin/orders`에서 송장을 읽기 전용으로 확인하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 149 | 데스크톱 송장 컬럼 | `/admin/orders` 데스크톱 테이블에 `송장` 컬럼이 보이고, 송장 보유 주문은 택배사와 운송장번호가 함께 표시된다 | [x] | 2026-05-29 로컬 seller fixture e2e 18/18 통과 |
| 150 | 송장 빈 값 | 송장 정보가 없는 기존 주문·직접배송·거점배송 주문은 `송장` 칸이 `-`로 표시된다 | [ ] | A1 |
| 151 | 모바일 송장 행 | 375px 폭 카드에서 스토어 ID 아래에 `송장` 행이 보이고 택배사·운송장번호가 카드 안에 수납된다 | [ ] | A1 |
| 152 | 모바일 긴 번호 | 긴 운송장번호가 카드 밖으로 넘치거나 가로 스크롤을 만들지 않는다 | [ ] | C7 |
| 153 | 기존 액션 회귀 | 강제환불 버튼, 상태 Badge, 금액 표시가 송장 행 추가 후에도 기존 위치와 동작을 유지한다 | [ ] | 회귀 |
| 154 | 자동 e2e | `admin-orders.spec.ts`에서 송장 보유 주문의 택배사·운송장번호 표시 테스트가 통과한다 | [x] | 2026-05-29 `SELLER_BASE=http://localhost:3011 pnpm --filter e2e test -- admin-orders.spec.ts` 18/18 통과 |

## §16. 판매자 주문 우선 알림 배너

> 출처: `seller-orders-improve-plan.md` §B 1차. `/orders` 진입 시 처리 필요 주문과 지연 주문을 먼저 보여주는 화면 내 알림 배너다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 155 | 배너 노출 | 처리 필요 또는 지연 주문이 있으면 주문 목록 상단에 `먼저 확인할 주문이 있습니다` 배너가 표시된다 | [ ] | §B 1차 |
| 156 | 처리 필요 이동 | `처리 필요 보기` 클릭 시 `처리 필요` 탭으로 이동하고 날짜 필터는 기본 `이번 주` 상태로 정리된다 | [ ] | §B 1차 |
| 157 | 지연 주문 이동 | `지연 주문 보기` 클릭 시 지연 주문이 있는 탭으로 이동하고 `지연` 날짜 그룹이 목록 최상단에 표시된다 | [ ] | 날짜 필터에 숨지 않음 |
| 158 | 공동구매 지연 기준 | 공구 토글에서 `groupDeliveryDate`가 지난 주문은 배너의 지연 건수에 포함된다 | [ ] | groupProductConfig 조인 |
| 159 | 빈 상태 회귀 | 처리 필요와 지연 주문이 모두 없으면 배너가 보이지 않고 기존 empty state와 탭 배치가 유지된다 | [ ] | 회귀 |
| 160 | 모바일 배치 | 375px 폭에서 배너 텍스트와 두 버튼이 카드 폭 안에 자연스럽게 줄바꿈되고 가로 스크롤을 만들지 않는다 | [ ] | 육안 검증 |
| 161 | 공구 탭 밖 지연 주문 | 공구 토글에서 현재 탭 밖(`대기 중`·`배송 중`)의 지연 주문도 배너 지연 건수에 포함되고 `지연 주문 보기`가 해당 탭으로 이동한다 | [ ] | §B 보강 |

## §17. 판매자 주문 일괄 준비 시작

> 출처: `seller-orders-improve-plan.md` §C 1차. 여러 주문 일괄 처리 중 송장번호와 충돌하지 않는 `준비 시작`만 먼저 처리하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 162 | 체크박스 노출 범위 | `ACCEPTED`·`CONFIRMED` 주문 카드에만 선택 체크박스가 보이고 `PREPARING` 이후 주문에는 보이지 않는다 | [ ] | §C 1차 |
| 163 | 액션 바 표시 | 준비 가능 주문이 있는 목록 상단에 준비 가능 건수, 선택 건수, 전체 선택, 선택 해제, `준비 시작` 버튼이 표시된다 | [ ] | §C 1차 |
| 164 | 선택 상태 정리 | 탭·판매 유형·날짜 필터 변경으로 대상 주문이 목록에서 사라지면 선택 상태도 함께 정리된다 | [ ] | 회귀 |
| 165 | 확인 모달 | 선택 후 `준비 시작` 클릭 시 선택 건수를 포함한 확인 모달이 열리고 취소하면 API 요청이 발생하지 않는다 | [ ] | §C 1차 |
| 166 | 일괄 처리 결과 | 확인 시 선택 주문들이 `PREPARING`으로 변경되고, 성공 건은 선택 해제되며 성공·부분 실패 알림이 표시된다 | [ ] | API 기존 상태 전이 사용 |
| 167 | 모바일 배치 | 375px 폭에서 액션 바, 체크박스, 카드 내용, 확인 모달 버튼이 겹치거나 가로 스크롤을 만들지 않는다 | [ ] | 육안 검증 |

## §18. 판매자 주문 일괄 택배 발송

> 출처: `seller-orders-improve-plan.md` §C 2차. `PREPARING` 택배 주문을 여러 건 선택하고, 주문별 송장 입력 후 배송 완료 처리하는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 168 | 체크박스 노출 범위 | `대기 중` 탭에서 `PREPARING`·택배 주문 카드에만 선택 체크박스가 보이고 직배송·거점 주문에는 보이지 않는다 | [ ] | §C 2차 |
| 169 | 액션 바 표시 | 택배 발송 가능 주문이 있으면 목록 상단에 가능 건수, 선택 건수, 전체 선택, 선택 해제, `택배 발송` 버튼이 표시된다 | [ ] | §C 2차 |
| 170 | 주문별 송장 모달 | 선택 후 `택배 발송` 클릭 시 선택 주문마다 택배사 Select와 운송장번호 입력창이 있는 모달이 열린다 | [ ] | §C 2차 |
| 171 | 기타 택배사 검증 | 주문별 택배사 `기타` 선택 시 직접 입력 필드가 보이고, 직접 입력값 없이는 발송할 수 없다 | [ ] | 단건 발송과 동일 규칙 |
| 172 | 필수값 차단 | 모든 선택 주문의 택배사와 3자 이상 운송장번호가 채워져야 `N건 발송 완료` 버튼이 활성화된다 | [ ] | API 가드와 동일 |
| 173 | 일괄 처리 결과 | 확인 시 성공 주문은 `DELIVERED`로 변경되고 선택 해제되며, 일부 실패 시 성공·실패 건수 알림이 표시된다 | [ ] | 기존 단건 API 반복 호출 |
| 174 | 모바일 배치 | 375px 폭에서 액션 바, 체크박스, 송장 모달 행, 버튼 영역이 겹치거나 가로 스크롤을 만들지 않는다 | [ ] | 육안 검증 |

## §19. 어드민 주문 상세 읽기 전용 모달

> 출처: `admin/admin-tab-orders-plan.md` B-9. F3 1차로 새 상세 API 없이 `/admin/orders` 목록 응답에 포함된 필드를 모달에서 읽기 전용으로 펼쳐 보여주는 흐름이다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 175 | 데스크톱 상세 버튼 | `/admin/orders` 데스크톱 테이블 각 행 우측에 상세 보기 아이콘이 보이고 클릭 시 `주문 상세` 모달이 열린다 | [ ] | F3 1차 |
| 176 | 상세 필드 표시 | 모달에서 주문번호·상태·상품·수량·금액·구매자·배송지·송장·생성일이 읽기 전용으로 표시되고 없는 값은 `-`로 표시된다 | [ ] | 목록 응답 기반 |
| 177 | 환불 액션 공존 | 상세 보기 아이콘 추가 후에도 강제환불 버튼 노출·모달 동작이 기존과 동일하다 | [ ] | 회귀 |
| 178 | 모바일 상세 버튼 | 375px 폭 카드에서 `상세` 버튼이 금액·강제환불 버튼과 겹치지 않고 탭 가능하다 | [ ] | 모바일 카드 |
| 179 | 모바일 모달 배치 | 375px 폭에서 상세 모달의 라벨·값이 카드 폭 안에 줄바꿈되고 가로 스크롤을 만들지 않는다 | [ ] | 육안 검증 |
| 180 | 닫기 동작 | ESC·외부 클릭·닫기 버튼으로 상세 모달을 닫아도 목록 필터·자동 새로고침 상태가 유지된다 | [ ] | 상태 회귀 |
| 181 | 자동 e2e | `admin-orders.spec.ts`에서 상세 모달 노출과 핵심 필드 표시 테스트가 통과한다 | [ ] | 2026-05-29 20건 수집 완료. 로컬 `SELLER_BASE=http://localhost:3014` 재실행은 `AUTH_SECRET`·`NEXT_PUBLIC_API_URL` 보정 후에도 Firebase 공개 env 미비로 `/admin/orders` 렌더가 `This page couldn’t load`에서 차단되어 새 코드 반영 프리뷰/로컬 인증 환경에서 재실행 필요 |

## §20. 어드민 주문 정렬·커서 페이지네이션

> 출처: `admin/admin-tab-orders-plan.md` B-10. F5 1차로 `/admin/orders`의 200건 고정 조회를 `createdAt` 정렬과 cursor `더 보기` 흐름으로 바꾼다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 182 | 정렬 기본값 | 최초 진입 시 정렬 Select가 `최신순`이고 첫 페이지가 최신 주문부터 표시된다 | [ ] | F5 1차 |
| 183 | 오래된순 전환 | 정렬을 `오래된순`으로 바꾸면 목록이 첫 페이지부터 다시 조회되고 오래된 주문부터 표시된다 | [ ] | `sort=createdAt_asc` |
| 184 | 페이지 크기 전환 | 페이지 크기 25·50·100 변경 시 목록이 첫 페이지부터 다시 조회되고 버튼·카운트가 새 크기에 맞게 표시된다 | [ ] | `limit=` |
| 185 | 더 보기 | `더 보기` 클릭 시 기존 목록 아래에 다음 주문들이 이어 붙고 중복 주문이 보이지 않는다 | [ ] | `cursor=` |
| 186 | 필터 공존 | 스토어·상태 필터 적용 상태에서 정렬·더 보기를 사용해도 필터 밖 주문이 섞이지 않는다 | [ ] | 회귀 |
| 187 | 새로고침 공존 | 수동 새로고침과 자동 새로고침은 현재 필터·정렬·페이지 크기를 유지하면서 첫 페이지를 다시 불러온다 | [ ] | T5 회귀 |
| 188 | 모바일 배치 | 375px 폭에서 스토어 Select, 상태 Select, 정렬 Select, 페이지 크기 Select, `더 보기` 버튼이 겹치거나 가로 스크롤을 만들지 않는다 | [ ] | 육안 검증 |

## §21. 어드민 초대 발급 내역 보기 개선

> 출처: `admin/admin-tab-invite-plan.md` 세션 S-A(T1~T3). 행별 복사, 발급일·사용일 표시, clipboard 폴백을 추가했다.
> 자동 검증은 통과했으나 로컬 dev 서버는 기존 환경 문제(`AUTH_SECRET` 부재, Firebase `auth/invalid-api-key`)로 `/admin/invite` 실제 화면 진입이 `/login` 이후 차단되어 브라우저 육안 확인은 운영/프리뷰에서 진행한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 189 | 데스크톱 행별 복사 | `/admin/invite` 데스크톱 테이블에서 모든 토큰 행에 복사 아이콘 버튼이 보이고, 클릭 시 해당 행 토큰만 복사됨 상태로 바뀐다 | [ ] | T1 |
| 190 | 모바일 카드 복사 | 375px 폭 모바일 카드에서도 토큰 옆 복사 아이콘이 카드 폭 안에 수납되고 모든 상태 행에서 탭 가능하다 | [ ] | T1 |
| 191 | 발급일·사용일 표시 | 데스크톱 테이블에 `발급일`, `사용일` 컬럼이 보이고 값은 `MM-DD HH:mm` KST 포맷, 미사용 토큰의 사용일은 `-`로 표시된다 | [ ] | T2 |
| 192 | 모바일 카드 일시 표시 | 모바일 카드에 발급·사용·만료 정보가 세로로 표시되고 긴 토큰·Badge·복사 버튼과 겹치지 않는다 | [ ] | T2 |
| 193 | 발급 직후 복사 회귀 | 새 토큰 발급 직후 기존 복사 버튼도 정상 동작하며, 행별 복사 상태와 서로 잘못 연동되지 않는다 | [ ] | T1 회귀 |
| 194 | clipboard 폴백 | 브라우저 clipboard 권한 거부/비지원 상황에서 textarea 폴백을 시도하고, 폴백도 실패하면 빨간 notification이 표시된다 | [ ] | T3 |

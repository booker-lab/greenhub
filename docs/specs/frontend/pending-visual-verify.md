# 미검증 육안 검증 통합 — 정산 status 필터 + 버튼 크기 + 어드민 반응형 + 겸직 역할 분리

> 통합: 2026-05-25 (세션88 후) · 출처 4건 합본
> - 세션86 #245 정산 [주문별 상세] status 필터 UI → 본문 **§1-V**
> - 세션87 #234/#CL-50 버튼 크기 단일화 → 본문 **§1-B**
> - 세션88 #246/#247/#CL-51 어드민 5테이블 모바일 반응형 → 본문 **§2**
> - 세션89 #CL-52 겸직 계정(admin+seller) 역할 분리 → 본문 **§3**
> - 세션95 #CL-55 PR-D 수수료 `NumberInput` 교체 → 본문 **§4 #61~#66**
> - 2026-05-29 #CL-55 어드민 소비자(users) 탭 개선 S1~S5 → 본문 **§5 #67~#79**
> - 2026-05-29 #CL-55 어드민 정산(settlements) 일괄 지급 SDD → 본문 **§6 #80~#89**
> - 2026-05-29 #CL-55 어드민 정산(settlements) 일괄 지급 T-F2a~e 구현 → 본문 **§6 #80~#90**
> - 2026-05-29 #CL-55 어드민 주문(orders) 세션α T1·T2 → 본문 **§10 #116~#120**
> - 2026-05-29 #CL-55 어드민 초대(invite) 세션 S-A T1~T3 → `pending-visual-verify-20260529.md` **§21 #189~#194**
>
> 성격: 모든 항목이 **코드·빌드 정합성(tsc/biome/build)은 종결**, 남은 것은 **브라우저 실사용 화면 육안 확인**(환경 제약상 사용자 영역).
> 셀러앱 A~F 마스터(세션83 M-PATH 완주·종결)는 `seller-refactor-visual-verify.md`에 그대로 둠 — 본 문서는 **아직 검증 안 된 작업**만 모음.
>
> 검증 표기: 통과 `[x]` · 실패 `[ ]`(메모란에 현상 기재) · 해당없음 `[-]`

---

## 검증 환경 (공통)

| 항목 | 값 |
|------|-----|
| 로그인 | 운영은 **카카오만** (환경 제약: `reference_visual_verify_env`) |
| 운영 DB | 단일 운영 DB(green-e4fe3) |
| 권장 뷰포트 | 모바일 PWA 폭 — §1은 ≤480px, §2는 ≤480px 권장(전환 기준은 768px) |
| 데스크톱 대조 | §2 한정: ≥768px(노트북 풀폭)에서 기존 테이블 그대로인지 동시 확인 |

### 섹션별 진입·계정

| 섹션 | 진입 경로 | 계정 | 시드 |
|------|----------|------|------|
| §1-V 정산 status 필터 | 정산 관리 → **[주문별 상세]** 탭 | 셀러(카카오) | status 혼합 필수 — `seed-settlements-visual` 등으로 pending/confirmed/paid/cancelled 혼합 |
| §1-B 버튼 크기 | 주문 목록(`/orders`) → "준비 시작" 가능 주문 → 상세(`/orders/[id]`) | 셀러(카카오) | — |
| §2 어드민 반응형 | `/admin/**` 각 화면 | **어드민 권한(role=admin)** 카카오 | 운영 단일 DB |
| §3 겸직 역할 분리 | 설정 탭 ↔ `/admin/**` | **겸직(role=admin + 자기 store 보유)** 카카오 / 대조용 일반 셀러 | 운영 단일 DB |

---

# §1. 셀러 — 정산 status 필터 + 버튼 크기

> 변경 모두 **로직·API 불변, UI 표현만 변경**.

## §1-V. 정산 status 필터 UI (세션86 #245)

> 비교 기준: 리팩토링 전 화면 = status 탭 없이 CSV 버튼 + 목록만.

### V-T1 — 필터 탭 렌더 (5탭)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 1 | [주문별 상세] 탭 진입 직후 | 목록 위에 status 탭 줄 노출 (`전체 / 정산 대기 / 확정 / 지급 완료 / 취소`) | [x] | 2026-05-31 `https://seller.greenlove.co.kr/settlements` 첨부 화면에서 목록 위 5탭 노출 확인 |
| 2 | 탭 라벨 문구 | shared SSOT `STATUS_LABEL`과 정확히 일치 (정산 대기·확정·지급 완료·취소) | [x] | 2026-05-31 첨부 화면에서 `정산 대기 / 확정 / 지급 완료 / 취소` 문구 확인 |
| 3 | 초기 진입 시 활성 탭 | **전체**가 선택(밑줄·primary 색) | [x] | 2026-05-31 첨부 화면에서 `전체` primary 색과 밑줄 확인 |
| 4 | 탭 5개 순서 | 전체 → 정산 대기 → 확정 → 지급 완료 → 취소 | [x] | 2026-05-31 첨부 화면에서 순서 확인 |

### V-T2 — 모바일 가로 스크롤 (C4 핵심)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 5 | 모바일 폭(≤480px)에서 5탭 | 잘림·줄바꿈 없이 **가로 스크롤**로 모든 탭 접근 가능 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 `mobile` 회귀에서 `375px` 5탭 렌더와 가로 스크롤 가능 상태 확인 |
| 6 | 스크롤바 | 막대 스크롤바 미노출(`scrollbarWidth:none`)이나 좌우 스와이프는 동작 | [x] | 2026-06-01 `seller-settlements.spec.ts`에서 탭 컨테이너 `scrollWidth > clientWidth`, `scrollbarWidth === 'none'` 단언 통과 |
| 7 | 마지막 탭(취소)까지 | 가로 스크롤 끝에서 잘리지 않고 전체 라벨 표시 | [x] | 2026-06-01 `375px`에서 마지막 `취소` 탭 끝 정렬 후 접근과 문서 가로 넘침 0 확인 |

### V-T3 — 탭 전환 → 필터 동작 (C2)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 8 | **정산 대기** 탭 클릭 | 목록이 pending 건만 노출되도록 재조회 (네트워크 `?status=pending`) | [x] | 2026-05-31 운영에서 `정산 대기` 탭 전환 후 빈 결과 확인. 현재 pending 시드 0건 |
| 9 | **확정** 탭 클릭 | confirmed 건만 노출 (`?status=confirmed`) | [x] | 2026-05-31 운영에서 `확정` 2건만 노출 확인 |
| 10 | **지급 완료** 탭 클릭 | paid 건만 노출 (`?status=paid`) | [x] | 2026-05-31 운영에서 `지급 완료` 1건만 노출 확인 |
| 11 | **취소** 탭 클릭 | cancelled 건만 노출 (`?status=cancelled`) | [x] | 2026-05-31 운영에서 `취소` 1건만 노출 확인 |
| 12 | 탭 전환 중 | "불러오는 중..." 로딩 표시가 탭 **아래**에 노출되고, 탭 줄은 사라지지 않음 | [x] | 2026-05-31 운영 `정산 대기` 전환 직후 5탭 유지·탭 아래 `불러오는 중...` 노출 확인 |

### V-T4 — 전체 탭 회귀 (C3)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 13 | 다른 탭 → **전체** 복귀 | 모든 status 합산 목록으로 복귀 (status 미전달 = 기존 전체 조회와 동일) | [x] | 2026-05-31 운영 `취소` → `전체` 복귀 후 확정 2·지급 완료 1·취소 1 합산 4건 노출 확인 |
| 14 | 전체 탭 목록 내용 | 리팩토링 전 [주문별 상세] 전체 목록과 동일(회귀 0) | [x] | 2026-05-31 운영 초기 전체 목록과 상태별 필터 후 전체 복귀 목록이 동일한 4건임을 확인 |

### V-T5 — 빈 결과·CSV (C6)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 15 | 해당 status 건이 0건인 탭 | "정산 완료된 주문이 없습니다" 메시지가 **탭 아래** 노출(탭 줄은 유지) | [x] | 2026-05-31 운영 `정산 대기` 빈 결과에서 메시지와 5탭 유지 확인 |
| 16 | 빈 결과 상태에서 다른 탭 | 탭이 사라지지 않아 곧바로 다른 status로 전환 가능 | [x] | 2026-05-31 운영 `정산 대기` 빈 결과에서 `확정` 탭으로 즉시 전환 확인 |
| 17 | 목록 존재 시 CSV 다운로드 버튼 | 기존과 동일하게 목록 우상단 노출·동작 | [x] | 2026-05-31 첨부 화면에서 목록 우상단 노출 및 `settlements_all_all.csv` 다운로드 완료 확인 |

## §1-B. 버튼 크기 단일화 (세션87 #234 / #CL-50)

> 변경: 상세 footer 3버튼 `size="lg"`→`size="md"` (카드 `sm` 유지, radius·fullWidth 불변)
> 핵심: 카드→상세 이동 시 같은 "준비 시작" 버튼의 **크기 점프가 완화**(2단계→1단계 차)됐는지 눈으로 확인

### B-1 — 카드→상세 크기 점프 완화 (핵심)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 18 | 주문 카드의 "준비 시작" 버튼 | 목록 카드 내 작은(`sm`) 버튼으로 노출 | [x] | 2026-05-31 운영 `RESET-reset-order-faraway` 목록 카드에서 `준비 시작` 버튼 높이 `36px` 확인 |
| 19 | 카드 클릭 → 상세 footer "준비 시작" | footer 버튼이 이전(`lg`)보다 한 단계 작아져(`md`) 점프가 줄어듦 | [x] | 2026-05-31 운영 `RESET-reset-order-faraway` 상세 footer에서 `준비 시작` 버튼 높이 `42px` 확인 |
| 20 | 점프 체감 | 카드↔상세 왕복 시 크기 변화가 거슬리지 않을 정도로 완화 | [x] | 2026-05-31 운영 목록 `36px` → 상세 `42px`로 높이 차이가 `6px`에 그쳐 과도한 크기 점프 없음 확인 |

### B-2 — footer 3버튼 일관성 (C2)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 21 | 상세 footer 준비 시작·택배 발송 완료·강제 취소 | 3버튼 모두 **동일 크기(`md`)**·풀폭으로 정렬 | [x] | 2026-05-31 운영 `RESET-reset-order-faraway`, `RESET-reset-order-parcel` 상세에서 준비 시작·택배 발송 완료·강제 취소가 모두 `42px` 높이·풀폭으로 정렬됨을 확인. 택배 상세의 `수거 예정 시각 Invalid Date`는 VF-007로 분리했고, seller 운영 배포 후 `5월 23일 오후 05:31` 정상 표시를 확인해 종결 |
| 22 | 모바일 폭 터치 타깃 | `md`+풀폭으로 손가락 탭 충분히 가능(너무 작지 않음) | [-] | 로그인된 seller 화면의 `375px` 뷰포트 제어 수단이 없어 향후 모바일 인증 묶음으로 이관 |

### B-3 — 준비 폼 진입 정합 (부수 효과)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 23 | footer "준비 시작" → 준비 폼 진입 | 폼의 "준비 시작 확인"·"취소" 버튼(`md`)과 footer 버튼 크기가 자연스럽게 이어짐(점프 없음) | [x] | 2026-05-31 운영 `RESET-reset-order-faraway` 상세에서 준비 폼을 열어 `준비 시작 확인`·`취소`가 모두 `42px` 높이로 이어짐을 확인. API 호출 없이 취소 |

---

# §2. 어드민 — 모바일 반응형 (세션88 #246/#247/#CL-51)

> 5개 테이블(settlements·orders·stores·invite·users) 카드형 전환.
> 폭 전환 기준: breakpoint `sm` = **768px**. 768px 미만=카드, 이상=테이블.
> 방법: 브라우저 DevTools 디바이스 툴바(폭 ≤480px 권장) 또는 실기기 PWA. 데스크톱 대조는 ≥768px.

## §2-A. 모바일(<768px) — 카드형 노출·버튼 접근 (핵심 결함 해소)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 24 | 정산(`/admin/settlements`) | 카드 노출. 각 카드: 스토어ID·상태 Badge·정산일시·거래/수수료·지급액 표시 | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 회귀에서 카드 필수 정보와 Badge 노출 확인 |
| 25 | 정산 — `confirmed` 카드 [지급처리] 버튼 | 버튼 노출·탭 가능 (← **원래 결함, 최우선**) | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 `mobile` 회귀에서 confirmed 카드 `지급처리` 2개 노출 확인 |
| 26 | 정산 — `confirmed` 외 상태 카드 | 지급처리 버튼 미노출(기존 로직 유지) | [x] | 2026-06-01 같은 `mobile` 회귀에서 전체 5건 중 confirmed 2건에만 `지급처리` 노출 확인 |
| 27 | 주문(`/admin/orders`) | 카드 노출. 주문번호·상태 Badge·스토어·금액 표시 | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 회귀에서 카드 필수 정보와 Badge 노출 확인 |
| 28 | 주문 — 환불 가능 상태 카드 [강제환불] | ACCEPTED/RECRUITING/CONFIRMED/PREPARING 카드에 버튼 노출·동작 | [-] | 세션δ `#136~#137`에서 일반 단계와 위험 단계 계약으로 확장·대체됨. 2026-06-02 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 PREPARING·DELIVERING·REVIEWED 카드의 강제환불 버튼 3개 노출, 일반 단계 무사유 처리, 위험 단계 사유 필수 처리를 자동 검증 |
| 29 | 스토어(`/admin/stores`) | 카드 노출. 상호·ID·상태 Badge·수수료율·[수수료 설정] 버튼 표시 | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 회귀에서 카드 필수 정보와 액션 노출 확인 |
| 30 | 스토어 — [수수료 설정] 탭 | 인라인 입력+[저장]/[취소] 정상 동작(편집 폼 카드 내 표시) | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 `mobile` 회귀에서 카드 내 입력·저장·취소 노출 확인 |
| 31 | 초대(`/admin/invite`) | 발급내역 카드 노출. 토큰·상태 Badge(유효/사용됨/만료)·만료일 표시 | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 회귀에서 3개 상태 카드와 일시 노출 확인 |
| 32 | 사용자(`/admin/users`) | 카드 노출. 이름·이메일·상태 Badge·[정지]/[복구] 버튼 표시 | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 회귀에서 정상·정지 카드와 액션 노출 확인 |
| 33 | 사용자 — [정지]/[복구] 탭 | ConfirmModal 정상 표출·처리 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 모바일 카드 복구 확인창 취소와 검색·정지 필터 적용 상태의 복구 확인 후 `suspended:false` 요청 본문을 검증 |

## §2-B. 모바일 — 레이아웃 품질

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 34 | 5개 화면 가로 스크롤 | 발생 0 (카드가 폭 안에 수납) | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 `mobile` 회귀에서 settlements·orders·stores·invite·users `scrollWidth <= clientWidth` 확인 |
| 35 | 긴 텍스트(이메일·토큰) | 줄바꿈/말줄임 정상 — 카드 밖 넘침 0 | [x] | 2026-06-01 fixture 인증 회귀에서 사용자 이메일·초대 토큰 포함 5개 화면 `scrollWidth <= clientWidth` 확인 |
| 36 | 빈 결과 메시지 | "정산 내역이 없습니다" 등 카드 분기에서도 깨지지 않음 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 스토어 ID 불일치 필터 적용 후 `정산 내역이 없습니다.` 문구, 필터·탭 유지, 테이블 미노출, 가로 넘침 0을 자동 검증 |
| 37 | 로딩 표시 | "불러오는 중..." 정상 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 `pending` 조회 지연 중 `불러오는 중...` 문구, 필터·탭 유지, 가로 넘침 0과 응답 후 카드 복귀를 자동 검증 |

## §2-C. 데스크톱(≥768px) — 회귀 0 (DOM 불변이라 안전, 확인용)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 38 | 5개 화면 테이블 레이아웃 | 기존 그대로 — 컬럼·정렬·여백·Badge 위치 변화 0 | [x] | 2026-06-01 운영 `1920px`에서 판매자·소비자·주문·정산 테이블 유지 확인. 초대는 운영 토큰 0건 빈 상태 |
| 39 | 데스크톱에서 카드 | 보이지 않음(`hiddenFrom="sm"` 정상 동작) | [x] | 2026-06-01 운영 `1920px`에서 판매자·소비자·주문·정산·초대 화면의 모바일 카드 미노출 확인 |
| 40 | 모바일에서 테이블 | 보이지 않음(`visibleFrom="sm"` 정상 동작) | [x] | 2026-06-01 최신 고정 프리뷰 fixture 인증 `mobile` 회귀에서 5개 화면 테이블 숨김 확인 |

## §2-D. 전환 경계

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 41 | 폭 768px 전후 리사이즈 | 카드↔테이블 전환이 자연스럽고 깜빡임/중복 노출 없음 | [x] | 2026-06-01 fixture 인증 회귀에서 5개 화면 `767px` 카드 유지·가로 넘침 0, `768px` 테이블 전환 확인 |

---

# §3. 겸직 계정(admin+seller) 역할 분리 (세션89 #CL-52)

> 배경: "어드민=store 없음" 전제로 만들어진 `proxy.ts` 가드가 겸직 계정(어드민 + 디어 오키드 판매자)의 사업자 프로필 수정을 막던 결함.
> 변경 3건: ① proxy 가드 `!storeId` 조건 추가 ② 설정 탭 "관리자 콘솔로 이동" 행(겸직만) ③ 어드민 헤더 "셀러 화면으로" 링크(겸직만).
> 노출 판정 SSOT = `role==='admin' && !!storeId`. 로그인 수단 무관.
> **계정 3종 교차 확인 필수**: 겸직(당신) / 일반 셀러 / (가능 시) 순수 어드민.
> **보안 주의**: "관리자 콘솔로 이동" 링크 숨김은 **UX**일 뿐 보안 아님. 실제 차단은 백엔드 — 셀러가 `/admin/*` 직접 입력해도 ② `admin/layout.tsx` `role!=='admin'→/orders` redirect, ③ `admin.controller` `@Roles('admin')`로 API 403. 링크 추가의 보안 영향 0(CRITICAL_LOGIC #CL-52 방어선 검증).

## §3-A. 겸직 계정 — 프로필 수정 진입 (핵심 결함 해소)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 42 | 설정 → "사업자 프로필 수정" 클릭 | `/onboarding` 진입, **디어 오키드 정보가 채워진 폼** 노출 (← 원래 결함: 어드민으로 튕김) | [x] | 2026-05-31 운영 겸직 계정(정연)에서 `/onboarding` 진입 및 난플렉스 기존 상호명·대표자명·연락처·소재지 조회 복구 확인. 사업자등록번호는 기존 저장값이 없는 선택 항목이라 빈 값. 저장 미실행. VF-004 종결 |
| 43 | 프로필 폼 저장 | 수정 후 [저장 후 시작하기] → `/orders`로 정상 이동·반영 | [-] | 2026-06-06 겸직 운영 계정 프로필 저장은 운영 데이터 쓰기이며, 되돌릴 수 있는 겸직 테스트 계정이 없어 이번 세션 조건에서는 확인 불가 처리 |

## §3-B. 겸직 계정 — 양방향 문(門) 노출·이동

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 44 | 설정 탭 "관리자" 섹션 | "관리자 콘솔로 이동" 행 노출 | [x] | 세션89 운영(`seller.greenlove.co.kr/settings`) 겸직 계정(정연)에서 노출 확인 |
| 45 | "관리자 콘솔로 이동" 클릭 | `/admin/stores`(어드민 콘솔)로 이동 | [x] | 2026-05-31 운영 겸직 계정에서 설정 링크 클릭 후 `/admin/stores` 이동 확인 |
| 46 | 어드민 헤더 우상단 | ADMIN 배지 옆 **"셀러 화면으로"** 링크 노출 | [x] | 2026-05-31 운영 `/admin/stores` 헤더에서 ADMIN 배지 옆 링크 노출 확인 |
| 47 | "셀러 화면으로" 클릭 | `/orders`(셀러 주문 화면)로 이동 | [x] | 2026-05-31 운영 헤더 링크 클릭 후 `/orders` 이동 확인 |
| 48 | 왕복 동선 | 설정↔어드민 양방향 URL 타이핑 없이 이동 가능 | [x] | 2026-05-31 운영 설정 → 어드민 → 셀러 주문 왕복 확인 |

## §3-C. 일반 셀러 — 회귀 0 (링크 미노출)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 49 | 일반 셀러(`seller@test.com` 류) 설정 탭 | "관리자" 섹션·"관리자 콘솔로 이동" 행 **미노출** | [x] | 2026-06-06 `TEST_SELLER_*`가 `role=seller`, `storeId` 보유 계정임을 확인하고 `seller-onboarding.spec.ts` chromium에서 설정 화면의 관리자 콘솔 링크 미노출을 검증 |
| 50 | 일반 셀러 "사업자 프로필 수정" | 기존대로 `/onboarding` 자기 프로필 정상 진입(회귀 0) | [x] | 2026-06-06 같은 chromium 회귀에서 설정의 `사업자 프로필 수정` 클릭 후 `/onboarding` 이동과 기존 상호명 pre-fill을 검증 |

## §3-D. 순수 어드민 — 회귀 0 (store 없는 운영자, 가능 시)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 51 | 순수 어드민(storeId 없음)이 `/onboarding` 접근 | 기존대로 `/admin/stores`로 리다이렉트(회귀 0) | [x] | 2026-06-06 `TEST_ADMIN_*`가 `role=admin`, `storeId` 없음 계정임을 확인하고 admin 전용 storageState로 `/onboarding` 접근 시 `/admin/stores` 리다이렉트를 검증 |
| 52 | 순수 어드민 헤더 "셀러 화면으로" | **미노출**(storeId 없으므로) | [-] | 순수 어드민 계정 없으면 해당없음 |

---

## §4. 어드민 판매자 "치우기"(아카이브) — 세션90 (#CL-53)

> 출처: `admin-store-archive-plan.md`(T1~T5·C1~C6) · 코드·빌드 정합성 종결, 남은 것은 브라우저 실사용 확인.
> 환경: 어드민 계정으로 `/admin/stores` 진입. **기록 없는 빈 판매자**(예: "디어 오키드" placeholder류)와 **기록 있는 운영 판매자**(난플렉스) 둘 다 필요.

> **e2e 자동 커버(읽기 전용):** `admin-store-archive.spec.ts` — 진입·상태 필터 기본값 "활성"·버튼 노출을
> 확인한다(세션90 기존 8테스트 통과, PR-B에서 토글 단언을 필터 단언으로 갱신). 운영 DB 보호 위해 **상태변경 클릭은 e2e에서 안 함** →
> 아래 #53/54/56/57(치우기·복구·차단)은 **육안 전용**으로 남음. #55(필터)·#59(모바일 버튼 노출)은 e2e가 노출까지 커버.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 53 | 빈 판매자 "치우기" 클릭 → 확인창 | "OO 판매자를 정리할까요? 주문·정산 기록은 보존됩니다." 다이얼로그 | [x] | 2026-06-06 `admin-stores-filter-sort.spec.ts` fixture에서 `디어 플라워` 치우기 클릭 시 확인창 문구를 검증 |
| 54 | 확인 후 | 목록에서 **사라짐**, 카운트 감소 | [x] | 2026-06-06 같은 fixture에서 `archive` 성공 후 재조회 목록에서 대상 판매자가 제거되고 활성 목록이 유지됨을 검증 |
| 55 | 상태 필터에서 "정리됨" 선택 | archived 판매자 **표시**, 배지 "정리됨"(회색) | [x] | 2026-06-06 fixture 기반 정리됨 필터에서 archived 판매자 행·배지와 URL 복원을 자동 검증 |
| 56 | archived 행 "복구" 클릭 | active 복귀, 상태 필터 "활성" 선택 시 일반 목록 노출 | [x] | 2026-06-06 fixture에서 `restore` 성공 후 활성 필터에 복구된 판매자가 다시 노출됨을 검증 |
| 57 | 기록 있는 판매자(난플렉스) "치우기" 시도 | 서버 차단 → "주문·정산 기록이 있는 판매자는 정리할 수 없습니다." notification | [x] | 2026-06-06 fixture에서 기록 보유 판매자 `archive` 400 응답과 서버 메시지 notification 노출을 검증 |
| 58 | archived 판매자의 과거 주문·정산 탭 조회 | 기록 **그대로 조회**(회귀 0) | [x] | 2026-06-06 `admin-store-archive.spec.ts` fixture에서 archived 판매자 상세가 `정리됨` 배지와 과거 주문 2건, 주문 금액, 플랫폼 수수료, 실지급 합계, 주문·정산 상태 버킷을 그대로 표시함을 검증 |
| 59 | 모바일(<sm) 카드에서 치우기/복구 버튼 | 노출·동작 정상(잘림 없음) | [x] | 노출=e2e mobile 통과, 동작(클릭)은 육안 |
| 60 | 검색·상태 필터·새로고침·정렬·URL 복원 | 필터 즉시 반영, 데스크톱 헤더/모바일 Select 정렬, 새로고침·뒤로가기 후 조건 보존 | [x] | 2026-05-31 운영에서 검색·URL·새로고침·뒤로가기 복원을 확인했다. 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 모바일 Select 수수료율 오름차순·내림차순 URL 전환과 직접 URL 검색어·상태 복원을 자동 검증 |
| 61 | 데스크톱 수수료 편집 입력칸 | Mantine `NumberInput` 노출, 테이블 행 높이 변화가 허용 범위(±1px) 내 | [x] | 2026-05-31 seller 운영 배포 `dpl_G5uNkgqeE7rgQkSCfYaFhwoWVacn` 후 난플렉스 행에서 입력·저장·취소 배치와 증감 제어 표시를 확인해 `VF-009` 종결 |
| 62 | 모바일 수수료 편집 입력칸 | 카드 안에서 height/padding 및 버튼 배치 깨짐 없음 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 카드 내 수수료 입력·저장·취소 노출과 문서 가로 넘침 0을 자동 검증 |
| 63 | 입력칸 focus 상태 | focus ring이 인접 셀·버튼과 충돌하지 않음 | [x] | 2026-05-31 운영 데스크톱에서 난플렉스 수수료 입력 focus ring과 인접 저장·취소 버튼 비충돌 확인 |
| 64 | 증감 제어와 모바일 입력 | `+`/`−` 조작 가능, 모바일에서 소수 키보드 노출·터치 접근성 확인 | [x] | 2026-05-31 운영 데스크톱에서 증감 제어 동작을 확인했다. 2026-06-02 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 `inputmode="decimal"`과 카드·테이블 양쪽 증감 제어 DOM 노출을 자동 검증 |
| 65 | 범위 차단 | `-0.1`, `1.5` 직접 입력이 차단되고 저장 API에 전달되지 않음 | [x] | 2026-05-31 운영 난플렉스 편집에서 `-0.1`, `1.5` 직접 입력이 칸에 남지 않는 것을 확인하고 취소. 저장 API 미호출 |
| 66 | 정상 저장 | `0.05` 입력·저장 후 표시값 `5.0%`, 기존 알림/저장 흐름 회귀 없음 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 `0.05` 저장 요청 본문과 저장 후 `5.0%` 표시를 자동 검증 |

> **2026-05-28 로컬 확인 차단 기록:** `http://localhost:3010/admin/stores` 진입을 시도했으나 로컬 `AUTH_SECRET` 부재로 `Auth.js MissingSecret`이 발생하고, 이어 `apps/seller/src/proxy.ts:14`에서 `session.user.role` 접근 오류가 발생해 입력 UI가 렌더되기 전에 중단됐다. 위 #61~#66은 인증 가능한 프리뷰 또는 운영 미적용 환경에서 확인한다.
>
> **2026-05-28 PR-E 자동 검증 기록:** 작업 트리 반영 seller 임시 프리뷰(`greenhub-seller-blkcqzhnf-jos-projects-d1cecc0c.vercel.app`, `READY`)에서 `admin-stores-filter-sort.spec.ts`가 `chromium`·`mobile` 합계 **16/16 통과**했다. fixture 기반으로 #55의 필터 전환, #60의 검색·정렬·새로고침·URL 복원 경로, #65의 `1.5` 저장 요청 차단을 자동 확인했다. 배지 색·뒤로가기 감각·`-0.1`·배치/실저장은 위 육안 체크를 유지한다.

---

# §5. 어드민 소비자(users) 탭 개선 — 세션 S1~S5 (#CL-55)

> 출처: `admin/admin-tab-users-plan.md` S1~S4. 로컬은 `AUTH_SECRET` 부재와 Firebase `auth/invalid-api-key`로 `/admin/users` 관리자 화면 렌더 전 차단되어, 인증 가능한 프리뷰 또는 운영 미적용 환경에서 확인한다.
> S5 자동화 `apps/e2e/tests/admin-users.spec.ts`는 route fixture 기반이라 운영 DB 쓰기 없이 검색·필터·새로고침·정지 모달을 커버한다. 단, 새 코드가 반영된 프리뷰/운영 URL에서 재실행해야 한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 67 | 가입일·전화 표시 | 데스크톱 테이블과 모바일 카드 모두 가입일(YYYY-MM-DD)·전화가 보이고 빈 전화는 `-`로 표시 | [x] | 2026-06-01 운영 데스크톱 확인과 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 가입일·전화·빈 전화 `-` 표시 확인 |
| 68 | 새로고침 버튼 | 헤더 우측 새로고침 아이콘 클릭 시 목록 재조회, 로딩 상태가 버튼에 표시 | [x] | 2026-05-31 운영 `/admin/users`에서 클릭 직후 버튼 비활성·`불러오는 중...` 표시, 완료 후 복귀 확인 |
| 69 | 검색 입력 | 이름·이메일·전화번호 부분일치 검색, 전화는 하이픈 입력 여부와 무관하게 매칭 | [x] | 2026-06-01 운영 데스크톱에서 이름·이메일 검색을 확인했고, 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 저장값 `01099998888`를 입력값 `010-9999-8888`로 검색해 하이픈 정규화 매칭을 검증 |
| 70 | 상태 필터 | 전체·정상·정지 탭 전환 시 행/카드가 즉시 필터링되고 검색어와 함께 적용 | [x] | 2026-06-01 운영 데스크톱에서 `전체 23건 → 정상 23건 → 정지 0건 → 전체 23건` 전환과 빈 결과 분기를 확인 |
| 71 | 빈결과 분기 | 원본 데이터 없음은 "등록된 소비자가 없습니다.", 필터 결과 없음은 "검색 결과가 없습니다." 표시 | [x] | 2026-05-31 운영 원본 데이터 0건에서 `등록된 소비자가 없습니다.` 확인. 2026-06-01 운영 불일치 검색과 정지 0건 탭에서 `검색 결과가 없습니다.` 확인 |
| 72 | 로딩 중 필터 유지 | 목록 로딩 중에도 검색 입력과 상태 탭이 사라지지 않음 | [x] | 2026-05-31 운영 새로고침 로딩 중 검색 입력과 `전체·정상·정지` 탭 유지 확인 |
| 73 | 모바일 배치 | 검색 입력 1줄 + 상태 탭 1줄 + 카드가 375px 폭에서 겹침·가로 스크롤 없이 표시 | [x] | 2026-06-01 fixture 인증 회귀에서 검색 입력·상태 탭·카드 액션 노출과 가로 넘침 0 확인 |
| 74 | 정지/복구 확인창 | 필터·검색 적용 상태에서도 정지/복구 버튼과 ConfirmModal 동작이 기존과 동일 | [x] | 2026-06-01 운영 정상 소비자 정지 확인창을 취소로 닫았고, 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 검색·정지 필터 적용 상태의 복구 확인창과 `suspended:false` 요청 본문을 검증 |
| 75 | 최신순 정렬 | `/admin/users` 최초 목록이 가입일 기준 최신순으로 표시 | [x] | 2026-06-01 운영 23건의 가입일이 `2026-05-17 → 2026-04-06` 내림차순으로 표시됨을 확인 |
| 76 | 5000건 상한 영향 | 운영 소비자 수가 5000명 미만이거나, 초과 시 검색 누락 위험을 별도 SDD로 승격 | [x] | 2026-06-01 운영 소비자 23건으로 5000건 미만 확인 |
| 77 | Firestore 인덱스 | `users(role ASC, createdAt DESC)` 인덱스가 운영 프로젝트에 배포되어 목록 조회가 인덱스 오류 없이 성공 | [x] | 2026-06-01 운영 `/admin/users` 최초 최신순 23건 조회가 오류 없이 성공 |
| 78 | S5 e2e 재실행 | 새 코드 반영 프리뷰 또는 운영 배포 후 `pnpm --filter e2e test -- admin-users.spec.ts`가 통과 | [x] | 2026-06-02 최신 seller 고정 프리뷰 `greenhub-seller-2omkcjr8s...`에서 전화 하이픈 정규화와 복구 요청 본문 보강 후 chromium·mobile 18/18 통과. 운영 DB 쓰기 없음 |
| 79 | 정지 사용자 refresh 차단 수동 확인 | 정지된 소비자의 refresh token으로 `/auth/refresh` 호출 시 401, 정상 소비자 refresh는 200 | [-] | 2026-06-06 `pnpm --filter api test -- auth.service.spec.ts --runInBand` 6/6로 정지 401·정상 rotation 200 계약 재확인. 현재 환경에는 정지 소비자 refresh token 또는 별도 정지 테스트 계정이 없어 이번 세션 조건에서는 수동 확인 불가 처리 |

---

# §6. 어드민 정산(settlements) 일괄 지급 — F2 구현 반영 후 육안 확인 (#CL-55)

> 출처: `admin/admin-tab-settlements-bulk-pay-plan.md`. T-F2a~e 백엔드·프론트 구현이 반영된 프리뷰 또는 운영 미적용 환경에서 확인한다.
> 운영 DB 직접 쓰기 위험이 있으므로 실제 지급 처리 항목은 테스트 DB, fixture 프리뷰, 또는 되돌릴 수 있는 검증 데이터에서만 수행한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 80 | 체크박스 노출 | `/admin/settlements` 데스크톱 테이블과 모바일 카드에서 `confirmed` 정산만 선택 가능 | [x] | 2026-05-31 운영 데스크톱과 2026-06-01 fixture 인증 모바일 회귀에서 confirmed 활성·paid 비활성 확인 |
| 81 | 비대상 상태 제외 | `pending`·`paid`·`cancelled` 정산은 전체 선택과 개별 선택 대상에서 제외 | [x] | 2026-05-31 운영 전체 목록에서 paid·cancelled 체크박스 비활성, 전체 선택 시 confirmed 3건만 액션 바에 반영됨을 확인. pending은 현재 0건 |
| 82 | 전체 선택 | 현재 필터·목록의 선택 가능한 `confirmed` 건만 한 번에 선택되고, 해제도 정상 동작 | [x] | 2026-05-31 운영 전체 선택 시 confirmed 3건만 선택되고 `선택 해제` 클릭 후 0건 복귀 확인 |
| 83 | 액션 바 | 선택 건수와 지급 합계가 선택 상태와 정확히 일치하고 선택 해제 버튼이 동작 | [x] | 2026-05-31 운영 전체 선택에서 `선택 3건 · 지급 합계 99,500원`, 단건 선택에서 `선택 1건 · 지급 합계 47,500원`, 선택 해제 후 액션 바 제거 확인 |
| 84 | 확인 모달 | 일괄 지급 버튼 클릭 시 선택 건수·지급 합계를 다시 보여주는 ConfirmModal 표출 | [x] | 2026-05-31 운영 데스크톱에서 confirmed 1건 선택 후 `선택한 1건을 지급 완료 처리할까요? 지급 합계는 14,000원입니다.` 확인창을 확인하고 취소·선택 해제로 원복. 지급 요청 미실행 |
| 85 | 전부 성공 처리 | 확인 후 성공 알림, 목록 재조회, 대상 정산 `paid` 전환과 `paidAt` 표시 확인 | [x] | 2026-06-03 #CL-81 별도 SDD 후 데스크톱 행·모바일 카드에 지급 상태 보조 문구 추가. 로컬 fixture 인증 회귀에서 전부 성공 후 paid 탭 3건 복귀와 신규 `입금 완료 2026-05-29 12:00` 2건 표시 확인. chromium·mobile 24/24 통과 |
| 86 | 부분 실패 처리 | 성공 건은 반영되고 실패 건은 실패 사유와 함께 알림 또는 상세 영역에 표시 | [x] | 2026-06-02 fixture 인증 회귀에서 성공 건 목록 제거, 실패 건 선택 유지, 실패 사유 `검증용 실패 정산`, 부분 완료 알림을 자동 검증 |
| 87 | 중복 클릭 방지 | 요청 중 버튼 loading/disabled 처리로 중복 지급 요청이 발생하지 않음 | [x] | 2026-06-02 fixture 인증 회귀에서 지연 응답 중 ConfirmModal 확인 버튼 비활성화와 bulk 요청 1회 유지를 자동 검증 |
| 88 | 모바일 배치 | 375px 폭에서 체크박스·액션 바·모달·카드 내용이 겹치거나 가로 스크롤을 만들지 않음 | [x] | 2026-05-29 `#114` 통합 모바일 캡처와 2026-06-01 fixture 인증 카드 회귀에서 가로 넘침 0 재확인 |
| 89 | 단건 지급 회귀 | 기존 `confirmed` 단건 [지급처리] 버튼이 그대로 노출·동작 | [x] | 2026-06-01 운영 confirmed 3건의 `지급처리` 버튼 노출 확인. 실제 지급 요청은 운영 쓰기 금지로 미실행 |
| 90 | 일괄 지급 API 라우트 | 새 코드 반영 환경에서 `POST /admin/settlements/bulk-pay`가 admin 권한으로 200 응답을 반환하고 `{ ok, failed }` 형식을 유지 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 admin 세션의 `POST /admin/settlements/bulk-pay` 200 응답과 `{ ok, failed }` 분기 처리를 자동 검증 |
| 91 | KST 정산일시 표시 | 데스크톱 테이블과 모바일 카드의 정산일시가 shared `toDateStrKST(..., { hour, minute })` 기준 `YYYY-MM-DD HH:mm` 형식으로 표시 | [x] | 2026-06-01 운영 데스크톱과 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 `YYYY-MM-DD HH:mm` 형식 확인 |
| 92 | 조회 실패 배너 | `/admin/settlements` 조회 실패 시 빈 결과와 구분되는 빨간 Alert가 필터 아래에 노출되고 목록 영역은 기존 3분기 상태를 유지 | [x] | 2026-06-01 `admin-settlements.spec.ts`에 상태별 조회 실패 fixture를 추가했다. `confirmed` 2건 조회 후 `paid` 조회를 500으로 실패시키면 빨간 Alert와 `정산 목록 조회 중 오류 발생` 안내가 노출되고 직전 목록 2건이 유지됨을 자동 검증. 최신 seller 고정 프리뷰에서 chromium·mobile 포함 정산 spec 12/12 통과 |
| 93 | 합계 툴팁 | 합계 카드 라벨 hover/focus 시 “현재 필터 범위의 confirmed+paid 합계이며 pending·cancelled는 제외” 설명이 표시되고 카드 높이·모바일 배치가 흔들리지 않음 | [x] | 2026-06-01 운영 데스크톱에서 플랫폼 수수료 합계 카드 hover 시 설명 툴팁 노출과 카드 높이 유지를 확인. 모바일 배치는 `#88`, `#101`, `#108`에서 별도 확인 |
| 94 | 새로고침 버튼 | 헤더 우측 새로고침 아이콘 클릭 시 목록을 재조회하고 loading 상태가 버튼에 표시되며 기존 필터값은 유지 | [x] | 2026-06-01 운영 데스크톱에서 새로고침 클릭 직후 버튼 비활성·`불러오는 중...` 표시, 완료 후 5건 복귀 확인 |

---

# §7. 어드민 정산(settlements) status 필터 — N+4 그룹 B (#CL-55)

> 출처: `admin/admin-tab-settlements-plan.md` N+4 T4a/T4b/T5. 새 코드 반영 프리뷰 또는 운영 미적용 환경에서 확인한다.
> Firestore 인덱스는 코드상 이미 정의되어 있으나, 운영 프로젝트 배포 여부는 별도 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 95 | status 탭 노출 | `/admin/settlements` 필터 영역에 전체·정산 대기·확정·지급 완료·취소 탭이 표시되고 라벨이 판매자 정산 화면과 일치 | [x] | 2026-05-31 운영에서 5개 탭 라벨·순서 확인 |
| 96 | 전체 탭 기본값 | 최초 진입은 `status` 쿼리 없이 기존 전체 목록을 조회하고 로딩·빈결과 상태에서도 탭이 유지 | [x] | 2026-05-31 운영 최초 전체 5건, 정산 대기 빈 결과에서도 5개 탭 유지 확인 |
| 97 | 상태별 조회 | 각 status 탭 클릭 시 `/admin/settlements?status=<status>` 경로로 재조회되고 목록은 해당 상태만 표시 | [x] | 2026-05-31 운영 정산 대기 0건·확정 3건·지급 완료 1건·취소 1건·전체 5건 복귀 확인 |
| 98 | 복합 필터 유지 | status와 storeId·from·to를 함께 적용해도 정산일시 desc 정렬과 최대 500건 제한이 유지 | [x] | 2026-05-31 운영 `이번 달 + 확정 + 80189070-...` 조합에서 2026-05-23 → 2026-05-22 desc 순서로 2건 조회 확인 |
| 99 | 인덱스 오류 없음 | status 단독 및 storeId+status+기간 조합 조회가 Firestore 인덱스 오류 없이 성공 | [x] | 2026-05-31 운영 status 단독 전환과 `이번 달 + 확정 + storeId` 복합 조회 모두 오류 없이 성공 |
| 100 | 선택 상태 정리 | status 탭 전환 후 이전 목록에서 선택한 일괄 지급 체크 상태가 현재 목록의 `confirmed` 대상만 남도록 정리 | [x] | 2026-05-31 운영 전체에서 confirmed 1건 선택 후 지급 완료 탭 전환 시 선택 0건 복귀 확인 |
| 101 | 모바일 배치 | 375px 폭에서 status 탭이 가로 스크롤로 표시되고 날짜·스토어 필터와 카드 내용이 겹치지 않음 | [x] | 2026-05-29 `#114` 통합 모바일 캡처에서 status 탭·필터·카드 공존과 가로 넘침 0 확인 |

---

# §8. 어드민 정산(settlements) DatePicker·빠른 기간 — N+5 그룹 C (#CL-55)

> 출처: `admin/admin-tab-settlements-plan.md` N+5 T6. 실제 프리뷰 또는 운영 미적중 환경에서 `/admin/settlements` 필터 영역을 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 102 | 빠른 기간 버튼 노출 | 스토어 ID 필터 옆에 `이번 주`·`이번 달`·`지난달` 버튼이 보이고 클릭 가능한 상태로 표시 | [x] | 2026-05-31 운영에서 3개 버튼 노출·클릭 확인 |
| 103 | 이번 주 KST 범위 | `이번 주` 클릭 시 KST 기준 월요일부터 오늘까지 from/to가 설정되고 목록이 해당 기간으로 재조회 | [x] | 2026-05-31 운영에서 `이번 주` 클릭 후 `2026-05-25 → 2026-05-31`, 빈 결과 재조회 확인 |
| 104 | 이번 달·지난달 범위 | `이번 달`은 월초~오늘, `지난달`은 이전 달 1일~말일로 설정되고 활성 버튼 스타일이 바뀜 | [x] | 2026-05-31 운영에서 `이번 달` `2026-05-01 → 2026-05-31` 5건, `지난달` `2026-04-01 → 2026-04-30` 빈 결과와 활성 상태 확인 |
| 105 | DatePicker 직접 선택 | 시작일·종료일 DatePicker로 날짜를 직접 고르면 기존 `from`·`to` 쿼리 배선이 그대로 동작 | [x] | 2026-05-31 운영에서 시작일·종료일을 각각 `2026-05-22`로 직접 선택해 목록이 5건 → 3건 → 2건으로 재조회되고 합계가 함께 갱신됨을 확인. 이후 `이번 달`로 복귀 |
| 106 | 날짜 clear 동작 | 각 DatePicker의 clear 버튼으로 값을 비우면 해당 필터가 제거되고 목록 재조회가 정상 동작 | [x] | 2026-05-31 운영 지난달 범위에서 시작일·종료일 clear를 순서대로 눌러 날짜 필터 제거와 전체 목록 복귀 확인 |
| 107 | 복합 필터 공존 | status 탭, storeId, 빠른 기간 또는 직접 선택 날짜를 함께 적용해도 선택 상태·일괄 지급 UI가 깨지지 않음 | [x] | 2026-05-31 운영 `이번 달 + 확정 + storeId` 조합에서 2건 조회와 지급처리 UI 유지 확인. 직접 선택 날짜는 `#105`로 별도 잔여 |
| 108 | 모바일 배치·스타일 | 375px 폭에서 빠른 기간 버튼과 DatePicker가 줄바꿈되어 겹치지 않고 Mantine 날짜 팝오버 스타일이 정상 로드 | [x] | 2026-05-29 `#114` 통합 모바일 캡처에서 빠른 기간·DatePicker 배치와 가로 넘침 0 확인 |

---

# §9. 어드민 정산(settlements) 통합 회귀 — N+6 e2e 반영 후 육안 확인 (#CL-55)

> 출처: `admin/admin-tab-settlements-plan.md` N+6 E3/E5. `apps/e2e/tests/admin-settlements.spec.ts`는 route fixture 기반으로 status 탭, 일괄 지급, 부분 실패, 새로고침, 빠른 기간 쿼리를 자동 확인한다.
> 실제 프리뷰 또는 운영 미적중 환경에서는 아래 항목을 육안으로 확인해 자동화가 놓치는 배치·포커스·알림 감각을 보완한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 109 | N+6 e2e 재실행 | 새 코드 반영 프리뷰 또는 운영 미적중 환경에서 `pnpm --filter e2e test -- admin-settlements.spec.ts`가 chromium·mobile 모두 통과 | [x] | 2026-05-31 최신 seller 고정 preview(`greenhub-seller-qnv9oht7p...`, SHA `14426f8`, READY)에서 10/10 통과. 운영 DB 쓰기 없음 |
| 110 | status 전환 회귀 | 전체·정산 대기·확정·지급 완료·취소 탭 전환 시 URL·목록·선택 상태가 자연스럽게 동기화 | ✅ | 2026-05-29 동일 preview에서 `SELLER_BASE=https://greenhub-seller-e0w9ozr5s-jos-projects-d1cecc0c.vercel.app pnpm --filter e2e test -- admin-settlements.spec.ts` 재실행 10/10 통과 |
| 111 | 일괄 지급 성공/부분 실패 알림 | 성공·부분 실패·전체 실패 케이스에서 알림 제목과 실패 사유가 잘리지 않고, 성공 건 선택만 해제 | ✅ | 2026-05-29 fixture 프리뷰 e2e 재실행 10/10 통과. 부분 실패 흐름은 운영 DB 쓰기 없이 route fixture로 확인 |
| 112 | 새로고침과 단건 지급 공존 | 새로고침 직후에도 단건 지급 ConfirmModal, 일괄 지급 액션 바, status 필터가 서로 상태를 꼬지 않음 | ✅ | 2026-05-29 fixture 프리뷰 e2e 재실행 10/10 통과, 데스크톱 캡처 `apps/e2e/test-results/admin-settlements-visual/desktop-action-bar.png` 확인 |
| 113 | 빠른 기간·DatePicker 복합 조합 | 이번 주·이번 달·지난달 버튼과 직접 날짜 선택을 status·storeId와 섞어도 쿼리와 표시값이 일치 | ✅ | 2026-05-29 fixture 프리뷰 e2e 재실행 10/10 통과, 액션 바 캡처에서 status·빠른 기간·DatePicker 동시 노출 확인 |
| 114 | 모바일 통합 배치 | 375px 폭에서 status 탭, 빠른 기간 버튼, DatePicker, 체크박스, 액션 바, 모달이 겹치거나 가로 스크롤을 만들지 않음 | ✅ | 2026-05-29 `mobile-action-bar.png`, `mobile-modal.png` 육안 확인. `documentElement.scrollWidth/clientWidth = 375/375`로 문서 가로 스크롤 0 |
| 115 | E1/E2 자동 정합성 재검증 | shared build/test, 4앱 tsc, API unit test, seller·consumer·driver·api build, 정산 변경 파일 biome check가 모두 통과 | ✅ | 2026-05-29 완료. §9 #110~#114 fixture 프리뷰 통합 확인도 완료 |

---

## §10. 어드민 주문(orders) 세션α — OrderStatus 타입화·강제환불 가드 (#CL-55)

> 출처: `admin/admin-tab-orders-plan.md` 세션α T1·T2. UI 변경은 의도하지 않았고, 운영 반영 후 기존 주문 탭 표시 회귀와 백엔드 우회 차단을 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 116 | 주문 상태 표시 회귀 | `/admin/orders` 데스크톱 테이블과 모바일 카드에서 기존 상태 라벨·색이 그대로 표시 | [x] | 2026-06-01 운영 데스크톱 확인과 최신 고정 프리뷰 fixture 인증 모바일 회귀에서 공통 Badge 렌더의 준비중·배달중 라벨 확인 |
| 117 | 상태 필터 회귀 | 상태 Select의 전체·11개 상태 옵션이 기존 순서와 라벨로 표시되고 선택 시 해당 상태만 조회 | [x] | 2026-06-01 운영 Firestore `status + createdAt DESC` 인덱스 배포와 seller 운영 배포 후 재검증. 전체+11개 옵션 순서·라벨 유지, `취소됨` 선택 시 `RESET-reset-order-cancelled` 1건만 조회됨 |
| 118 | 기존 환불 버튼 노출 범위 | ACCEPTED·RECRUITING·CONFIRMED·PREPARING 주문에만 기존 강제환불 버튼이 표시되고 CANCELLED는 미노출 | [-] | 세션δ `#137`에서 위험 상태 노출 계약으로 대체됨. 2026-05-31 운영 데스크톱에서 `DELIVERED` 강제환불 버튼과 `CANCELLED` 미노출을 확인 |
| 119 | 위험 상태 직접 호출 차단 | 새 코드 반영 환경에서 `DELIVERING` 주문을 사유 없이 `POST /admin/orders/:id/refund` 호출하면 400과 “배달 후 환불은 사유(5자 이상)가 필수입니다.” 응답 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 `order-delivering-beta` 직접 호출의 400 응답과 오류 문구를 자동 검증 |
| 120 | 위험 상태 정상 사유 처리 | 위험 상태 주문도 5자 이상 사유를 보내면 환불 처리되고 `cancelReason`에 trim된 사유가 저장 | [x] | 2026-06-02 최신 고정 프리뷰 fixture 인증 회귀에서 `order-delivering-beta` 강제환불 모달의 4자 차단과 5자 이상 사유 요청 본문을 자동 검증. 운영 DB 쓰기 없음 |

---

## §11. 주문 취소 경로 세션β — 셀러·소비자·자동 취소 경로 회귀 (#CL-55)

> 출처: `admin/admin-tab-orders-cancel-paths.md`. 세션β 조사 결과 추가 가드 구현은 없지만, 기존 경로의 버튼 노출·읽기 전용·취소 사유 표시 회귀를 운영 반영 후 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 121 | 셀러 일반 단계 취소 버튼 | 셀러 주문 상세에서 `ACCEPTED`·`CONFIRMED`·`PREPARING` 주문에만 `강제 취소` 버튼이 보이고, 사유 5자 미만이면 확인이 비활성화 | [-] | 운영 `ACCEPTED` 버튼·4자 차단, `PREPARING` 버튼 노출 확인. `CONFIRMED` 운영 데이터 부재로 향후 테스트 데이터 묶음 이관 |
| 122 | 셀러 위험 단계 취소 미노출 | `DELIVERING`·`HUB_ARRIVED`·`PICKED_UP`·`DELIVERED`·`REVIEWED` 주문에서는 `강제 취소` 버튼이 없고 읽기 전용 안내만 표시 | [-] | 운영 `DELIVERED` 미노출·반품 안내 확인. 나머지 위험 상태 운영 데이터 부재로 향후 테스트 데이터 묶음 이관 |
| 123 | 소비자 공동구매 취소 버튼 | 소비자 주문 상세에서 `RECRUITING` 공동구매 주문에만 `공동구매 참여 취소` 버튼이 보이고, 취소 후 취소 상태 안내가 표시 | [x] | 2026-06-03 환경 변수 게이트 fixture에서 실제 소비자 상세 컴포넌트의 취소 확인창·PATCH 1회·취소 안내 전환을 chromium·mobile로 검증 |
| 124 | 소비자 확정 이후 취소 미노출 | `CONFIRMED` 이후 소비자 주문 상세에는 공동구매 취소 버튼이 보이지 않고 기존 상태 타임라인이 유지 | [x] | 2026-06-03 같은 fixture에서 `CONFIRMED` 공동구매의 취소 버튼 미노출과 배송 현황·주문 확정 타임라인 유지를 검증 |
| 125 | 결제 실패·타임아웃 취소 표시 | 결제 실패 또는 15분 초과 `PENDING` 타임아웃 주문이 소비자 주문 상세에서 `주문 취소`와 사유(`payment_failed`·`timeout`)로 표시 | [x] | 2026-06-03 같은 fixture에서 `CANCELLED` 주문의 `payment_failed`·`timeout` 사유와 모바일 가로 넘침 0을 검증 |
| 126 | 공동구매 미달 자동 취소 표시 | 최소 수량 미달 자동 취소 주문이 소비자 주문 상세와 셀러 주문 상세에서 `CANCELLED`와 `목표 수량 미달성으로 취소` 사유로 표시 | [x] | 2026-06-03 consumer 실제 상세 컴포넌트와 seller 실제 `OrderInfoSection` fixture에서 양쪽 사유 표시를 chromium·mobile로 검증 |

---

## §12. 어드민 주문(orders) 세션γ — 스토어 Select·새로고침 UX (#CL-55)

> 출처: `admin/admin-tab-orders-plan.md` 세션γ T4·T5. 스토어 ID 직접 입력을 Select로 교체하고, 수동 새로고침과 30초 자동 새로고침 토글을 추가했다. 운영 또는 인증 가능한 프리뷰에서 레이아웃과 실제 재조회 동작을 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 127 | 스토어 Select 기본값 | `/admin/orders` 최초 진입 시 스토어 필터가 `전체 스토어`로 보이고 주문 목록이 전체 조회된다 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 128 | 스토어 옵션 표시 | 활성 스토어가 이름 기준 옵션으로 표시되고 선택 시 `/admin/orders?storeId=<id>` 재조회 결과만 남는다 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 129 | 치운 스토어 토글 off | 기본 상태에서는 치운 스토어 옵션과 `(치운)` 라벨이 보이지 않는다 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 130 | 치운 스토어 토글 on | `치운 스토어 포함`을 켜면 archived 스토어가 `(치운)` 라벨과 함께 옵션에 나타난다 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 131 | 치운 스토어 선택 후 off | 치운 스토어를 선택한 뒤 토글을 끄면 선택값이 `전체 스토어`로 정리되고 목록이 재조회된다 | [x] | 2026-06-02 최신 seller 고정 프리뷰 fixture 인증 모바일 회귀에서 archived 선택 후 토글 off 시 Select `전체 스토어` 표시·전체 목록 복원과 마지막 재조회 URL의 archived `storeId` 제거 확인 |
| 132 | 상태 필터 공존 | 스토어 Select와 상태 Select를 함께 바꿔도 쿼리와 목록이 서로 꼬이지 않는다 | [x] | 2026-06-01 운영 Firestore `storeId + status + createdAt DESC` 인덱스 배포 후 재검증. 난플렉스 선택 시 7건, 이어 `준비중` 선택 시 해당 주문 3건만 조회됨 |
| 133 | 수동 새로고침 버튼 | 헤더 우측 새로고침 아이콘 클릭 시 현재 스토어·상태 필터를 유지한 채 목록을 다시 불러오고 loading 상태가 표시된다 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 134 | 자동 새로고침 토글 | `자동 새로고침(30초)`을 켜면 30초마다 현재 필터 조건으로 재조회되고, 끄면 추가 재조회가 멈춘다 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 135 | 모바일 배치 | 375px 폭에서 스토어 Select, 상태 Select, 치운 스토어 토글, 자동 새로고침 토글, 새로고침 아이콘이 겹치거나 가로 스크롤을 만들지 않는다 | [x] | 2026-06-02 같은 모바일 회귀에서 archived 선택·복원 흐름, 자동 새로고침 토글·새로고침 아이콘 노출과 문서 가로 넘침 0 확인 |

---

## §13. 어드민 주문(orders) 세션δ — 강제환불 모달 (#CL-55)

> 출처: `admin/admin-tab-orders-plan.md` 세션δ T6. native `prompt()`를 Mantine 모달로 교체하고, 일반 단계와 위험 단계를 분리했다. 운영 또는 인증 가능한 프리뷰에서 실제 환불 쓰기는 테스트 데이터로만 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 136 | 일반 단계 모달 | `ACCEPTED`·`RECRUITING`·`CONFIRMED`·`PREPARING` 주문의 강제환불 클릭 시 경고 배너 없이 모달이 열리고, 사유 빈 값으로 확인 가능 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 137 | 위험 단계 버튼 노출 | `DELIVERING`·`HUB_ARRIVED`·`PICKED_UP`·`DELIVERED`·`REVIEWED` 주문에도 강제환불 버튼이 표시 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 138 | 위험 단계 검증 | 위험 단계 모달에 빨간 경고 배너가 보이고, 사유 5자 미만에서는 `환불 처리` 버튼이 비활성화 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 139 | 위험 단계 정상 처리 | 위험 단계에서 사유 5자 이상 입력 후 확인하면 요청이 전송되고 백엔드 가드와 동일하게 처리 | [x] | 2026-05-29 로컬 seller fixture e2e 16/16 통과 |
| 140 | 닫기 동작 | ESC·외부 클릭·취소 버튼으로 모달을 닫아도 환불 요청이 전송되지 않고 처리중 상태가 남지 않음 | [x] | 2026-05-31 운영 데스크톱에서 `DELIVERED` 위험 단계 모달의 ESC·외부 클릭 닫기와 `ACCEPTED` 일반 단계 모달의 취소 닫기를 확인. 환불 요청 미실행 |
| 141 | 모바일 배치 | 375px 폭에서 모달 제목, 경고 배너, Textarea, 버튼 영역이 잘리거나 겹치지 않음 | [x] | 2026-06-02 최신 seller 고정 프리뷰 fixture 인증 회귀에서 위험 단계 모달 제목·경고·Textarea·취소·환불 처리 버튼 노출과 모달·문서 가로 넘침 0 확인. 주문 spec chromium·mobile 30/30 통과 |
| 142 | 세션δ e2e 재실행 | 새 코드 반영 프리뷰 또는 운영 미적중 환경에서 `pnpm --filter e2e test -- admin-orders.spec.ts`가 chromium·mobile 모두 통과 | [x] | 2026-05-31 최신 seller 고정 preview(`greenhub-seller-qnv9oht7p...`, SHA `14426f8`, READY)에서 20/20 통과. 운영 DB 쓰기 없음 |

---

## §14. 어드민 드라이버(drivers) status 서버 필터 배선·타입 정규화 (#CL-55)

> 출처: `admin/admin-tab-drivers-plan.md` S1(T1)·S2(T2). 백엔드 status 필터는 이미 존재하며, 탭 전환 시 프론트 hook이 `/admin/drivers?status=...`를 호출하도록 배선하고 클라이언트 필터를 제거했다. S2는 `getDrivers` 타입 좁히기와 `suspended` 옵셔널 분기 명시로 런타임 변경은 의도하지 않는다. 실제 계정·운영 데이터가 필요한 항목은 코드 반영 프리뷰 또는 운영 미적용 환경에서 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 211 | 승인 대기 탭 진입 | `GET /admin/drivers?status=pending` 호출, 응답 명단과 화면 카드 명단이 1:1 일치 | [x] | 2026-05-29 로컬 최신 seller fixture e2e 통과. seller 프리뷰 branch alias `greenhub-seller-git-codex-admin-st-4007d3-jos-projects-d1cecc0c.vercel.app` READY |
| 212 | 승인 완료 탭 진입 | `GET /admin/drivers?status=approved` 호출, 승인 완료 드라이버만 노출 | [x] | 2026-05-29 로컬 최신 seller fixture e2e 통과. 프리뷰 e2e는 Vercel Authentication 401로 차단 |
| 213 | 정지 탭 진입 | `GET /admin/drivers?status=suspended` 호출, 정지 드라이버만 노출 | [x] | 2026-05-29 로컬 최신 seller fixture e2e 통과 |
| 214 | 전체 탭 진입 | `GET /admin/drivers`처럼 status 쿼리 없이 호출, 정지 포함 전체 명단 노출 | [x] | 2026-05-29 로컬 최신 seller fixture e2e 통과 |
| 215 | 승인 액션 후 목록 | 승인 대기 카드 승인 후 현재 탭에서 카드가 사라지고 승인 완료 탭에서 확인 가능 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 승인 후 승인 대기 탭 제거와 승인 완료 탭 이동 확인 |
| 216 | 정지 액션 후 목록 | 승인 완료 카드 정지 후 현재 탭에서 카드가 사라지고 정지 탭에서 확인 가능 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 정지 후 승인 완료 탭 제거·정지됨 탭 이동과 정지 해제 후 승인 완료 탭 복귀 확인 |
| 217 | 로딩·빈결과 상태 | 로딩/빈결과에서도 4개 탭 UI가 유지되고 탭 전환 가능 | [x] | 2026-05-31 운영 `/admin/drivers`에서 승인 완료·정지됨 빈 결과와 승인 대기 새로고침·탭 전환 로딩 중에도 4개 탭 유지 및 전환 가능 확인 |
| 218 | 시각 회귀 | 데스크톱·모바일에서 탭, 카드, 버튼 겹침·가로 스크롤·텍스트 넘침 0 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 `375px` 탭·카드·상태별 액션과 문서 가로 넘침 0 확인 |
| 219 | S2 타입 정규화 회귀 | 승인 대기·승인 완료·정지 카드의 배지와 승인/정지/정지 해제 버튼 노출이 S1과 동일 | [x] | 2026-05-29 e2e에서 배지·정지 해제 버튼 노출 확인 |
| 220 | 가입일 표시 | 각 드라이버 카드 이메일 아래에 `가입일 YYYY-MM-DD` 또는 `가입일 -`가 표시되고 KST 날짜 포맷을 사용 | [x] | 2026-06-01 운영 `/admin/drivers` 승인 대기 카드에서 이메일 아래 `가입일 2026-05-17`, `가입일 2026-05-16` 형식 표시 확인 |
| 221 | 가입일 모바일 배치 | 375px 폭에서 이름·배지·이메일·가입일·액션 버튼이 겹치거나 잘리지 않음 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 `pending-driver@example.com`, `가입일 2026-05-29`, 승인·정지 버튼 수납 확인 |
| 222 | 드라이버 검색 입력 | 이름 또는 이메일 일부를 입력하면 현재 status 탭의 서버 응답 목록 안에서 일치 카드만 남고, 검색어를 지우면 원래 목록으로 복귀 | [x] | 2026-05-31 운영 승인 대기 탭에서 이메일 일부 `1778944207639` 검색 시 1건만 노출되고 검색어 제거 시 전체 목록 복귀 확인 |
| 223 | 검색 빈결과 분기 | 원본 목록 없음은 `드라이버가 없습니다.`, 검색 결과 없음은 `검색 결과가 없습니다.`로 구분 표시 | [x] | 2026-05-31 운영 승인 완료·정지됨 탭 원본 빈 상태와 승인 대기 탭 불일치 검색 빈 상태의 문구 분리 확인 |
| 224 | 새로고침 버튼 | 헤더 우측 새로고침 아이콘 클릭 시 현재 status 탭과 검색어 UI를 유지한 채 목록을 다시 불러오고 loading 상태가 버튼에 표시 | [x] | 2026-05-31 운영 승인 대기 탭에서 이메일 검색어를 유지한 채 새로고침 클릭 후 `불러오는 중...`, `새로고침` 표시와 동일 1건 복귀 확인 |
| 225 | 로딩 중 필터 유지 | 새로고침 또는 탭 전환 로딩 중에도 검색 입력과 4개 status 탭이 사라지지 않음 | [x] | 2026-05-31 운영 새로고침과 승인 완료 탭 전환 직후 로딩 중에도 검색 입력값과 4개 탭 유지 확인 |
| 226 | 검색·새로고침 모바일 배치 | 375px 폭에서 제목, 새로고침 아이콘, 검색 입력, status 탭, 카드 액션 버튼이 겹침·가로 스크롤 없이 표시 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 제목·새로고침·검색·4개 탭·카드 액션과 문서 가로 넘침 0 확인 |
| 227 | 액션 버튼 노출 회귀 | 승인 대기 카드는 `승인`·`정지`, 승인 완료 카드는 `정지`, 정지 카드는 `정지 해제` 버튼만 기존과 동일하게 표시 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 세 상태별 액션 노출 계약 확인 |
| 228 | 액션 버튼 스타일 회귀 | `승인`은 초록 기본 버튼, `정지`는 빨강 light 버튼, `정지 해제`는 회색 light 버튼으로 기존 스타일 유지 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 `data-variant`와 브라우저 계산 배경색으로 세 버튼 스타일 확인 |
| 229 | 액션 확인창 회귀 | 각 버튼 클릭 시 기존 ConfirmModal 제목·본문·확인 라벨·색상이 그대로 표시 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 승인·정지·정지 해제 확인창의 제목·본문·확인 라벨·계산 배경색 확인 |
| 230 | 처리 중 라벨 회귀 | 액션 처리 중인 카드의 해당 버튼은 `처리중…`으로 바뀌고 중복 클릭이 막힘 | [x] | 2026-06-01 최신 seller 고정 프리뷰 fixture 인증 회귀에서 지연 정지 응답 동안 카드의 두 액션이 `처리중…` 비활성 상태로 바뀌고 응답 후 성공 알림으로 복귀함을 확인 |

## §15. 어드민 배너(banner) 탭 결함 해소 — T1·T2·T3·T5a·T6 (#CL-55)

> 출처: `admin/admin-tab-banner-plan.md` G-7. 저장 전 CTA 비대칭 입력 차단, 저장·업로드 실패 알림, 서버 배너 hydrate 의존성 교정, CTA 모바일 1열 배치를 반영했다. 인증 가능한 프리뷰 또는 운영 미적중 환경에서 확인한다.

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 231 | CTA 비대칭 입력 차단 | 버튼 문구만 입력하거나 URL만 입력한 상태에서 저장 시 저장 요청이 차단되고 빨간 알림과 입력 오류가 표시 | [x] | 2026-05-31 다중 배너 Drawer `#250`에서 문구만 입력 차단 확인. 저장 요청 없음 |
| 232 | CTA 전체 미사용 허용 | 버튼1·버튼2의 문구와 URL을 모두 비우면 저장 차단 없이 통과 | [x] | 2026-05-31 다중 배너 Drawer `#250`에서 CTA 양쪽 빈 값은 CTA 오류 없이 통과 확인. 저장 요청 없음 |
| 233 | 저장 실패 알림 | 네트워크/API 실패 시 버튼 상태가 복구되고 실패 사유 알림이 표시 | [x] | 2026-06-02 `seller-banner.spec.ts` fixture에서 `/admin/banners` 저장 `500` 실패 사유 알림과 저장 버튼 상태 복구를 자동 검증 |
| 234 | 이미지 업로드 실패 알림 | 업로드 실패 시 업로드 상태가 종료되고 빨간 알림이 표시되며 파일 입력이 초기화 | [x] | 2026-06-02 Firebase Storage 업로드 `403` fixture에서 빨간 알림·인라인 오류, 로딩 종료, 파일 입력 초기화를 자동 검증 |
| 235 | 배너 hydrate 회귀 | 서버 배너 로드 후 입력 중인 값이 불필요한 effect 재실행으로 덮어써지지 않음 | [x] | 2026-06-01 운영 기본 배너 수정 Drawer에서 임시 헤드라인 입력과 라이브 미리보기 유지를 확인하고 ESC로 폐기 |
| 236 | CTA 모바일 배치 | 375px 폭에서 버튼 문구·링크 입력이 1열로 쌓이고 겹침·가로 스크롤이 없음 | [x] | 2026-06-02 `Drawer.css` 선택 import와 `size="min(780px, 100vw)"` 보완 후 최신 seller 프리뷰에서 CTA 4개 입력 1열·가로 넘침 0 자동 검증 |
| 237 | 이미지 업로드 가드 | PDF·GIF 또는 2MB 초과 이미지를 선택하면 업로드 요청 없이 빨간 알림과 인라인 오류가 표시되고, PNG/JPG/WebP 2MB 이하는 기존 업로드 흐름으로 진행 | [x] | 2026-06-01 운영 Drawer에서 PDF와 2MB 초과 JPG를 선택해 업로드 전 빨간 알림·인라인 오류 확인. Storage 쓰기 없음 |

## 검증 결과 요약 (검증 완료 후 기재)

| 섹션 | 결과 | 발견 결함 | 환경(store/시드/뷰포트) |
|------|------|----------|------------------------|
| §1-V 정산 status 필터 | | | |
| §1-B 버튼 크기 | | | |
| §2 어드민 반응형 | | | |
| §3 겸직 역할 분리 | | | |
| §4 판매자 치우기(아카이브) | | | |
| §5 소비자 탭 개선 | | | |
| §6 정산 일괄 지급 | | | |
| §7 정산 status 필터 | | | |
| §8 정산 DatePicker·빠른 기간 | | | |
| §9 정산 통합 회귀 | | | |
| §10 주문 세션α | | | |
| §11 주문 세션β | | | |
| §12 주문 세션γ | 통과 | #131·#135 fixture 인증 모바일 회귀 완료 | 최신 seller 고정 프리뷰, chromium·mobile e2e |
| §13 주문 세션δ 환불 모달 | 통과 | #140 운영 읽기 전용 확인, #141 fixture 인증 모바일 회귀 완료 | 운영 seller + 최신 seller 고정 프리뷰 |
| §19 주문 상세 모달 | 대기 | #175~#181 새 코드 반영 프리뷰 또는 인증 환경 확인 필요 | 새 코드 반영 프리뷰 또는 로컬 인증 환경 |
| §20 주문 정렬·커서 페이지네이션 | 대기 | #182~#188 운영/프리뷰 육안 확인 필요 | 새 코드 반영 프리뷰 또는 로컬 인증 환경 |
| §14 드라이버 status 서버 필터·가입일 | 부분 통과 | #215~#218, #220~#221은 운영/프리뷰 육안 확인 유지. T3 seller 프리뷰는 READY, 로컬 직접 진입은 인증 환경값 누락으로 차단 | 로컬 seller fixture chromium·mobile e2e 8/8, T3 seller preview `greenhub-seller-jzcy0ohgh-jos-projects-d1cecc0c.vercel.app` |
| §16 판매자 주문 우선 알림 배너 | 대기 | #155~#161 운영/프리뷰 육안 확인 필요 | 새 코드 반영 프리뷰 또는 로컬 인증 환경 |
| §17 판매자 주문 일괄 준비 시작 | 대기 | #162~#167 운영/프리뷰 육안 확인 필요 | 새 코드 반영 프리뷰 또는 로컬 인증 환경 |
| §18 판매자 주문 일괄 택배 발송 | 대기 | #168~#174 운영/프리뷰 육안 확인 필요 | 새 코드 반영 프리뷰 또는 로컬 인증 환경 |

---

## 관련 문서

- `settlement-status-filter-plan.md` — 정산 필터 아토믹 플랜(T1~T5·C1~C6)
- `button-size-unify-plan.md` — 버튼 크기 단일화 플랜(T1~T3·C1~C5)
- `admin-responsive-plan.md` — 어드민 반응형 플랜(#CL-51)
- `seller-refactor-visual-verify.md` — 셀러 육안 검증 마스터(세션83 M-PATH 종결, 형식 원본)
- `reference_visual_verify_env` 메모리 — 검증 환경 제약(카카오 로그인·단일 운영 DB·시드 스크립트)
- `admin/admin-tab-settlements-bulk-pay-plan.md` — 어드민 정산 일괄 지급 SDD
- `admin/admin-tab-orders-cancel-paths.md` — 주문 취소 경로 전수 조사

---

## 2026-05-29 추가 육안검증 묶음

500라인 가드를 지키기 위해 2026-05-29에 추가된 판매자 주문·어드민 주문·어드민 초대 검증 항목은 별도 파일로 분리했다.

- `pending-visual-verify-20260529.md` §14 #143~#148 — 판매자 주문 상세 택배 발송 운송장 입력
- `pending-visual-verify-20260529.md` §15 #149~#154 — 어드민 주문 목록 송장 표시
- `pending-visual-verify-20260529.md` §16 #155~#161 — 판매자 주문 우선 알림 배너
- `pending-visual-verify-20260529.md` §17 #162~#167 — 판매자 주문 일괄 준비 시작
- `pending-visual-verify-20260529.md` §18 #168~#174 — 판매자 주문 일괄 택배 발송
- `pending-visual-verify-20260529.md` §19 #175~#181 — 어드민 주문 상세 읽기 전용 모달
- `pending-visual-verify-20260529.md` §20 #182~#188 — 어드민 주문 정렬·커서 페이지네이션
- `pending-visual-verify-20260529.md` §21 #189~#194 — 어드민 초대 발급 내역 보기 개선
- `pending-visual-verify-20260529.md` §22 #195~#200 — 어드민 초대 토큰 취소
- `pending-visual-verify-20260529.md` §23 #201~#206 — 어드민 초대 토큰 prefix 검색
- `pending-visual-verify-20260529.md` §24 #207~#210 — 어드민 초대 취소 자동 검증 후 육안 확인

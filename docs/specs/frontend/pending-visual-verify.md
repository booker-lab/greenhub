# 미검증 육안 검증 통합 — 정산 status 필터 + 버튼 크기 + 어드민 반응형 + 겸직 역할 분리

> 통합: 2026-05-25 (세션88 후) · 출처 4건 합본
> - 세션86 #245 정산 [주문별 상세] status 필터 UI → 본문 **§1-V**
> - 세션87 #234/#CL-50 버튼 크기 단일화 → 본문 **§1-B**
> - 세션88 #246/#247/#CL-51 어드민 5테이블 모바일 반응형 → 본문 **§2**
> - 세션89 #CL-52 겸직 계정(admin+seller) 역할 분리 → 본문 **§3**
>
> 성격: 모든 항목이 **코드·빌드 정합성(tsc/biome/build)은 종결**, 남은 것은 **브라우저 실사용 화면 육안 확인**(환경 제약상 사용자 영역).
> 셀러앱 A~F 마스터(세션83 M-PATH 완주·종결)는 `seller-refactor-visual-verify.md`에 그대로 둠 — 본 문서는 **아직 검증 안 된 작업**만 모음.
>
> 검증 표기: 통과 `[x]` · 실패 `[ ]`(메모란에 현상 기재) · 해당없음 `[-]`

---

## 검증 환경 (공통)

| 항목 | 값 |
|------|-----|
| 로그인 | 운영은 **카카오만** (환경 제약: `reference_visual_verify_env`) |
| 운영 DB | 단일 운영 DB(green-e4fe3) |
| 권장 뷰포트 | 모바일 PWA 폭 — §1은 ≤480px, §2는 ≤480px 권장(전환 기준은 768px) |
| 데스크톱 대조 | §2 한정: ≥768px(노트북 풀폭)에서 기존 테이블 그대로인지 동시 확인 |

### 섹션별 진입·계정

| 섹션 | 진입 경로 | 계정 | 시드 |
|------|----------|------|------|
| §1-V 정산 status 필터 | 정산 관리 → **[주문별 상세]** 탭 | 셀러(카카오) | status 혼합 필수 — `seed-settlements-visual` 등으로 pending/confirmed/paid/cancelled 혼합 |
| §1-B 버튼 크기 | 주문 목록(`/orders`) → "준비 시작" 가능 주문 → 상세(`/orders/[id]`) | 셀러(카카오) | — |
| §2 어드민 반응형 | `/admin/**` 각 화면 | **어드민 권한(role=admin)** 카카오 | 운영 단일 DB |
| §3 겸직 역할 분리 | 설정 탭 ↔ `/admin/**` | **겸직(role=admin + 자기 store 보유)** 카카오 / 대조용 일반 셀러 | 운영 단일 DB |

---

# §1. 셀러 — 정산 status 필터 + 버튼 크기

> 변경 모두 **로직·API 불변, UI 표현만 변경**.

## §1-V. 정산 status 필터 UI (세션86 #245)

> 비교 기준: 리팩토링 전 화면 = status 탭 없이 CSV 버튼 + 목록만.

### V-T1 — 필터 탭 렌더 (5탭)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 1 | [주문별 상세] 탭 진입 직후 | 목록 위에 status 탭 줄 노출 (`전체 / 정산 대기 / 확정 / 지급 완료 / 취소`) | [ ] | |
| 2 | 탭 라벨 문구 | shared SSOT `STATUS_LABEL`과 정확히 일치 (정산 대기·확정·지급 완료·취소) | [ ] | |
| 3 | 초기 진입 시 활성 탭 | **전체**가 선택(밑줄·primary 색) | [ ] | |
| 4 | 탭 5개 순서 | 전체 → 정산 대기 → 확정 → 지급 완료 → 취소 | [ ] | |

### V-T2 — 모바일 가로 스크롤 (C4 핵심)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 5 | 모바일 폭(≤480px)에서 5탭 | 잘림·줄바꿈 없이 **가로 스크롤**로 모든 탭 접근 가능 | [ ] | |
| 6 | 스크롤바 | 막대 스크롤바 미노출(`scrollbarWidth:none`)이나 좌우 스와이프는 동작 | [ ] | |
| 7 | 마지막 탭(취소)까지 | 가로 스크롤 끝에서 잘리지 않고 전체 라벨 표시 | [ ] | |

### V-T3 — 탭 전환 → 필터 동작 (C2)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 8 | **정산 대기** 탭 클릭 | 목록이 pending 건만 노출되도록 재조회 (네트워크 `?status=pending`) | [ ] | |
| 9 | **확정** 탭 클릭 | confirmed 건만 노출 (`?status=confirmed`) | [ ] | |
| 10 | **지급 완료** 탭 클릭 | paid 건만 노출 (`?status=paid`) | [ ] | |
| 11 | **취소** 탭 클릭 | cancelled 건만 노출 (`?status=cancelled`) | [ ] | |
| 12 | 탭 전환 중 | "불러오는 중..." 로딩 표시가 탭 **아래**에 노출되고, 탭 줄은 사라지지 않음 | [ ] | |

### V-T4 — 전체 탭 회귀 (C3)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 13 | 다른 탭 → **전체** 복귀 | 모든 status 합산 목록으로 복귀 (status 미전달 = 기존 전체 조회와 동일) | [ ] | |
| 14 | 전체 탭 목록 내용 | 리팩토링 전 [주문별 상세] 전체 목록과 동일(회귀 0) | [ ] | |

### V-T5 — 빈 결과·CSV (C6)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 15 | 해당 status 건이 0건인 탭 | "정산 완료된 주문이 없습니다" 메시지가 **탭 아래** 노출(탭 줄은 유지) | [ ] | |
| 16 | 빈 결과 상태에서 다른 탭 | 탭이 사라지지 않아 곧바로 다른 status로 전환 가능 | [ ] | |
| 17 | 목록 존재 시 CSV 다운로드 버튼 | 기존과 동일하게 목록 우상단 노출·동작 | [ ] | |

## §1-B. 버튼 크기 단일화 (세션87 #234 / #CL-50)

> 변경: 상세 footer 3버튼 `size="lg"`→`size="md"` (카드 `sm` 유지, radius·fullWidth 불변)
> 핵심: 카드→상세 이동 시 같은 "준비 시작" 버튼의 **크기 점프가 완화**(2단계→1단계 차)됐는지 눈으로 확인

### B-1 — 카드→상세 크기 점프 완화 (핵심)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 18 | 주문 카드의 "준비 시작" 버튼 | 목록 카드 내 작은(`sm`) 버튼으로 노출 | [ ] | |
| 19 | 카드 클릭 → 상세 footer "준비 시작" | footer 버튼이 이전(`lg`)보다 한 단계 작아져(`md`) 점프가 줄어듦 | [ ] | |
| 20 | 점프 체감 | 카드↔상세 왕복 시 크기 변화가 거슬리지 않을 정도로 완화 | [ ] | |

### B-2 — footer 3버튼 일관성 (C2)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 21 | 상세 footer 준비 시작·택배 발송 완료·강제 취소 | 3버튼 모두 **동일 크기(`md`)**·풀폭으로 정렬 | [ ] | |
| 22 | 모바일 폭 터치 타깃 | `md`+풀폭으로 손가락 탭 충분히 가능(너무 작지 않음) | [ ] | |

### B-3 — 준비 폼 진입 정합 (부수 효과)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 23 | footer "준비 시작" → 준비 폼 진입 | 폼의 "준비 시작 확인"·"취소" 버튼(`md`)과 footer 버튼 크기가 자연스럽게 이어짐(점프 없음) | [ ] | |

---

# §2. 어드민 — 모바일 반응형 (세션88 #246/#247/#CL-51)

> 5개 테이블(settlements·orders·stores·invite·users) 카드형 전환.
> 폭 전환 기준: breakpoint `sm` = **768px**. 768px 미만=카드, 이상=테이블.
> 방법: 브라우저 DevTools 디바이스 툴바(폭 ≤480px 권장) 또는 실기기 PWA. 데스크톱 대조는 ≥768px.

## §2-A. 모바일(<768px) — 카드형 노출·버튼 접근 (핵심 결함 해소)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 24 | 정산(`/admin/settlements`) | 카드 노출. 각 카드: 스토어ID·상태 Badge·정산일시·거래/수수료·지급액 표시 | [ ] | |
| 25 | 정산 — `confirmed` 카드 [지급처리] 버튼 | 버튼 노출·탭 가능 (← **원래 결함, 최우선**) | [ ] | |
| 26 | 정산 — `confirmed` 외 상태 카드 | 지급처리 버튼 미노출(기존 로직 유지) | [ ] | |
| 27 | 주문(`/admin/orders`) | 카드 노출. 주문번호·상태 Badge·스토어·금액 표시 | [ ] | |
| 28 | 주문 — 환불 가능 상태 카드 [강제환불] | ACCEPTED/RECRUITING/CONFIRMED/PREPARING 카드에 버튼 노출·동작 | [ ] | |
| 29 | 스토어(`/admin/stores`) | 카드 노출. 상호·ID·상태 Badge·수수료율·[수수료 설정] 버튼 표시 | [ ] | |
| 30 | 스토어 — [수수료 설정] 탭 | 인라인 입력+[저장]/[취소] 정상 동작(편집 폼 카드 내 표시) | [ ] | |
| 31 | 초대(`/admin/invite`) | 발급내역 카드 노출. 토큰·상태 Badge(유효/사용됨/만료)·만료일 표시 | [ ] | |
| 32 | 사용자(`/admin/users`) | 카드 노출. 이름·이메일·상태 Badge·[정지]/[복구] 버튼 표시 | [ ] | |
| 33 | 사용자 — [정지]/[복구] 탭 | ConfirmModal 정상 표출·처리 | [ ] | |

## §2-B. 모바일 — 레이아웃 품질

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 34 | 5개 화면 가로 스크롤 | 발생 0 (카드가 폭 안에 수납) | [ ] | |
| 35 | 긴 텍스트(이메일·토큰) | 줄바꿈/말줄임 정상 — 카드 밖 넘침 0 | [ ] | |
| 36 | 빈 결과 메시지 | "정산 내역이 없습니다" 등 카드 분기에서도 깨지지 않음 | [ ] | |
| 37 | 로딩 표시 | "불러오는 중..." 정상 | [ ] | |

## §2-C. 데스크톱(≥768px) — 회귀 0 (DOM 불변이라 안전, 확인용)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 38 | 5개 화면 테이블 레이아웃 | 기존 그대로 — 컬럼·정렬·여백·Badge 위치 변화 0 | [ ] | |
| 39 | 데스크톱에서 카드 | 보이지 않음(`hiddenFrom="sm"` 정상 동작) | [ ] | |
| 40 | 모바일에서 테이블 | 보이지 않음(`visibleFrom="sm"` 정상 동작) | [ ] | |

## §2-D. 전환 경계

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 41 | 폭 768px 전후 리사이즈 | 카드↔테이블 전환이 자연스럽고 깜빡임/중복 노출 없음 | [ ] | |

---

# §3. 겸직 계정(admin+seller) 역할 분리 (세션89 #CL-52)

> 배경: "어드민=store 없음" 전제로 만들어진 `proxy.ts` 가드가 겸직 계정(어드민 + 디어 오키드 판매자)의 사업자 프로필 수정을 막던 결함.
> 변경 3건: ① proxy 가드 `!storeId` 조건 추가 ② 설정 탭 "관리자 콘솔로 이동" 행(겸직만) ③ 어드민 헤더 "셀러 화면으로" 링크(겸직만).
> 노출 판정 SSOT = `role==='admin' && !!storeId`. 로그인 수단 무관.
> **계정 3종 교차 확인 필수**: 겸직(당신) / 일반 셀러 / (가능 시) 순수 어드민.

## §3-A. 겸직 계정 — 프로필 수정 진입 (핵심 결함 해소)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 42 | 설정 → "사업자 프로필 수정" 클릭 | `/onboarding` 진입, **디어 오키드 정보가 채워진 폼** 노출 (← 원래 결함: 어드민으로 튕김) | [ ] | |
| 43 | 프로필 폼 저장 | 수정 후 [저장 후 시작하기] → `/orders`로 정상 이동·반영 | [ ] | |

## §3-B. 겸직 계정 — 양방향 문(門) 노출·이동

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 44 | 설정 탭 "관리자" 섹션 | "관리자 콘솔로 이동" 행 노출 | [ ] | |
| 45 | "관리자 콘솔로 이동" 클릭 | `/admin/stores`(어드민 콘솔)로 이동 | [ ] | |
| 46 | 어드민 헤더 우상단 | ADMIN 배지 옆 **"셀러 화면으로"** 링크 노출 | [ ] | |
| 47 | "셀러 화면으로" 클릭 | `/orders`(셀러 주문 화면)로 이동 | [ ] | |
| 48 | 왕복 동선 | 설정↔어드민 양방향 URL 타이핑 없이 이동 가능 | [ ] | |

## §3-C. 일반 셀러 — 회귀 0 (링크 미노출)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 49 | 일반 셀러(`seller@test.com` 류) 설정 탭 | "관리자" 섹션·"관리자 콘솔로 이동" 행 **미노출** | [ ] | |
| 50 | 일반 셀러 "사업자 프로필 수정" | 기존대로 `/onboarding` 자기 프로필 정상 진입(회귀 0) | [ ] | |

## §3-D. 순수 어드민 — 회귀 0 (store 없는 운영자, 가능 시)

| # | 확인 항목 | 통과 기준 | 결과 | 메모 |
|---|----------|----------|:----:|------|
| 51 | 순수 어드민(storeId 없음)이 `/onboarding` 접근 | 기존대로 `/admin/stores`로 리다이렉트(회귀 0) | [ ] | |
| 52 | 순수 어드민 헤더 "셀러 화면으로" | **미노출**(storeId 없으므로) | [-] | 순수 어드민 계정 없으면 해당없음 |

---

## 검증 결과 요약 (검증 완료 후 기재)

| 섹션 | 결과 | 발견 결함 | 환경(store/시드/뷰포트) |
|------|------|----------|------------------------|
| §1-V 정산 status 필터 | | | |
| §1-B 버튼 크기 | | | |
| §2 어드민 반응형 | | | |
| §3 겸직 역할 분리 | | | |

---

## 관련 문서

- `settlement-status-filter-plan.md` — 정산 필터 아토믹 플랜(T1~T5·C1~C6)
- `button-size-unify-plan.md` — 버튼 크기 단일화 플랜(T1~T3·C1~C5)
- `admin-responsive-plan.md` — 어드민 반응형 플랜(#CL-51)
- `seller-refactor-visual-verify.md` — 셀러 육안 검증 마스터(세션83 M-PATH 종결, 형식 원본)
- `reference_visual_verify_env` 메모리 — 검증 환경 제약(카카오 로그인·단일 운영 DB·시드 스크립트)
